# TapESTORY — decision & edge-case log

A static, dependency-free web rhythm game (GitHub Pages target): tap to the beat, and each tap paints in the next part of a picture-story. Good taps follow the story's good path; misses show a "fumbled" version of the same moment.

This file is the running log of decisions and edge cases. Append or update entries; mark superseded ones rather than deleting them.

---

## Decisions

### Product & scope

| # | Decision | Who | Why |
|---|---|---|---|
| D1 | Assets are **procedural first** (music synthesized in-browser, art drawn in-browser), with curated packs and free online APIs as later additions | User | Guarantees "never runs out of content" and "loads fast" with zero download and no licensing burden |
| D2 | Bad path = **shadow track**: every story beat has a good and a fumbled version of the *same* moment; the ending is chosen by final accuracy | User | The story never forks mid-song, so there are no illogical jumps between the good and bad storylines |
| D3 | The fumbled variant is a **transform, not a second asset** (re-render with perturbed parameters / a filter over the good panel) | Claude | Any art source gets bad-path art for free; hand-made bad panels would double curation forever |
| D4 | Rhythm style = **cue patterns** (call-and-response), not "tap every beat" | User | Matches the Rhythm Heaven feel the project is aiming for |
| D5 | Stack = **vanilla JS ES modules, no build step**, no external scripts or CDNs | User | Smallest payload, nothing to install, works when served as plain files from GitHub Pages |
| D6 | Milestones: **M1** procedural core → **M1.5** learnable minigames + art styles → **M2** user uploads → **M3** curated packs + online APIs | Claude | M1 exercises every hard part (clock, decode, latency) with nothing external to go wrong |
| D7 | Input = **tap + hold/release** (not tap-only, not tap+flick) | User | Adds depth without controls that are awkward on mouse or keyboard |
| D8 | Each game starts with a **short, skippable practice**, auto-skipped once that game type is cleared | User | Rhythm Heaven teaches its vocabulary before testing it |
| D9 | Progression = **level ladder + remixes** (every 4th game mixes the cues of cleared games) | User | Difficulty ramps, and remixes reward recognising learned cues |
| D10 | Art is **not limited to watercolour**: a style system (watercolour, paper cut-out, pixel, ink sketch, crayon, neon, geometric), each with its own fumble transform | User | Variety across games; styles are picked per game, weighted by minigame fit |
| D11 | Online APIs are used mainly through an **offline curation script** that commits static assets; any runtime fetching happens only on the menu screen, never during play | Claude | Runtime API calls contradict "load fast" and "never run out"; verified in-browser that Openverse works with CORS while AIC/CMA images do not |
| D12 | Uploaded files stay **local to the device** (File API only, never transmitted) | Claude | A static site has no server; this is a privacy property worth keeping explicit |

### Rhythm design (M1.5)

| # | Decision | Who | Why |
|---|---|---|---|
| D13 | Each game is **one minigame** with its own cue vocabulary, sound pack, groove and mascot | Claude, from user feedback that rhythms felt predictable with nothing to learn | The fun in Rhythm Heaven is learning one level's specific rhythms |
| D14 | Every cue sound has **exactly one meaning** within a game, and every judged event must be predictable from the audio alone — enforced by an automatic "learnability lint" | Claude | Otherwise "hard" degenerates into "unfair"; the lint keeps this true across every random seed |
| D15 | Difficulty ramps via tempo, subdivision (8ths → 16ths/triplets/swing), cue-to-response gap variety, density, fake-outs and cue combinations — **never** via events the player couldn't anticipate | Claude | "Unconventional but learnable" was the user's explicit target |
| D16 | The **player makes the response sound**: hit SFX are triggered live from pre-rendered buffers, and the baked per-note melody was removed from the song | Claude | The old song played the answer on every judged note, which removed both the challenge and the feedback |
| D17 | Song form teaches: practice → introduce cue A → vary A → introduce cue B → combine → breather → finale | Claude | Repetition is what makes a variation readable as a variation |

### Art style system (M1.5)

| # | Decision | Who | Why |
|---|---|---|---|
| D29 | Every style module exports `{ id, name, cost (1-3), renderScene(scene, rng, opts), renderFumbled(scene, rng, opts) }` from `js/story/styles/*.js`, registered in `js/story/styles/index.js` | Claude, per plan | Adding a style is a self-contained file plus one registry line; `js/story/loader.js` never branches on style name |
| D30 | Hero figures (fox/bird/boat/kite/snail/lantern) are one shared **abstract part list** (`js/story/styles/parts.js`): each part is `{role, shape: ellipse\|polygon\|dot\|path\|arc\|spiral, ...normalized coords, colorSlot}` in hero-size-normalized units | Claude, per plan | Every style walks the same list and draws each part in its own technique; a new style never needs hero-specific geometry |
| D31 | Scene composition (horizon/sun/prop/hero placement, palette-to-named-colour-slot resolution) is shared in `js/story/styles/scene.js` (`computeLayout`, `resolvePalette`), called by every style with the same rng draw order | Claude, per plan | Keeps "same seed → same composition, different rendering technique" true across styles; centralizes the fumble tilt/slump so every style gets it for free |
| D32 | `js/story/watercolor.js` + `js/story/fumble.js` were deleted; watercolour became `js/story/styles/watercolor.js` (renderScene/renderFumbled directly, no separate fumble-policy indirection) | Claude, per plan step 5 ("remove fumble.js if redundant") | Every style now owns its own fumble transform, so the old single-purpose indirection layer had no remaining callers |
| D33 | `loadStory()`'s `params` gained **optional** `stylePrefs` (style-id → weight) and `forceStyle`; `js/story/loader.js` also reads `?style=<id>` from `location.search` directly (not passed through by `main.js`) | Claude, per plan | Keeps `js/main.js`'s existing call (`{seed, numPhrases, regionCounts, lowEnd}`) working untouched while the rhythm-engine agent edits it in parallel; `?style=` is a debug override so it belongs at the source, not threaded through every caller |
| D34 | Low-end mode excludes cost-3 styles from the weighted pool **unless** a style is explicitly forced (param or `?style=`) | Claude, per plan | "Avoid the expensive ones on low-end" shouldn't silently override an explicit debug request |
| D35 | `tools/styles.html` renders every style × 3 seeds × 2 hero types (good+fumbled) plus a full-budget timing sample per style, reusing `generateStory()` + `subRng()` so it renders through the exact same code path as the real game | Claude, per plan step 6 | A contact sheet that doesn't use the real pipeline could pass while the game fails |
| D58 | **Round 2** (coordinator review): `scene.js`'s `computeLayout()` became `composeScene()` - a full shot-composition system (shot type: wide/mid/closeup/silhouette/highangle/detail; time-of-day day/golden/dusk/night; weather clear/rain/snow/fog; a far/mid/near parallax layer stack per environment type sea/forest/hills/mountains/town/path; hero third-placement, facing, near/far size) - every style now draws `composition.layers`/`celestial`/`stars`/`weatherParticles` through its OWN existing shape-fill primitive (`paintWash`/`paperShape`/`hatchFill`/`crayonFill`/`glowStroke`/`fillOutline`), so adding real scene variety didn't need bespoke per-style scenery code | Claude, per coordinator round-2 feedback ("every panel is the same postcard") | Reuses the exact pattern the M1.5 plan already established for hero parts (shared abstract shapes, per-style rendering) instead of inventing a second mechanism |
| D59 | `composeScene()`'s shot/weather/time-of-day/layer/hero-placement decisions are **independent of `opts.fumbled`** - only the hero's final pose (tilt/slump) differs | Claude | Preserves the existing "good and fumbled render the same composition" guarantee (needed for the shadow-track design, D2/D3) now that composition is much richer |
| D60 | `parts.js`'s fox and bird part lists were redrawn with much larger silhouette differences (fox: tall pointed ears, a big two-part fluffy tail, a snout bump; bird: a sharp swept-wing polygon, a long beak, a 3-feather tail fan) | Claude, per coordinator feedback ("a fox and a bird look like the same round blob") | Every style draws the same shared part list, so a bigger silhouette difference here fixes hero readability in all 7 styles at once |
| D61 | Pixel art's internal render resolution is genuinely tiny (112×~81 normal, 80×~58 low-end, vs. the previous 128/96) and the WHOLE internal frame goes through one Bayer-dither-then-quantize pass (14-colour palette) after all drawing, instead of quantizing each shape's fill colour individually | Claude, per coordinator feedback ("pixels aren't chunky enough") | A single whole-frame dither pass is what makes gradients (sky) *and* flat fills (ground/hero) all show genuine dither speckle, and is far less code than dithering every shape's fill separately |
| D62 | `tools/styles.html`'s canvases are mounted at their native rendered size (no CSS width/height downscale), with `image-rendering: pixelated` added specifically to the pixel style's canvases | Claude (bug found via coordinator's own review) | The previous contact sheet rendered pixel art at 300px and displayed it CSS-scaled to 150px, which the browser's default bilinear downscaling blurred back into a smooth gradient - the underlying bitmap was already correctly chunky, only the *preview* was lying about it |
| D63 | **Round 3**: crayon's `crayonFill` stroke COUNT is now derived from the shape's own area via a Poisson coverage model (`n = -ln(1-coverage) * boxArea / strokeArea`, capped by `maxStrokes` for perf) instead of a fixed count passed in by the caller | Claude, per coordinator feedback ("mostly WHITE with scattered coloured dashes") | A fixed stroke count doesn't scale with canvas size, so the SAME count that looked "busy" on a 150px contact-sheet thumbnail (round 2's over-correction) read as barely-there confetti at the game's real 1280px render size; deriving count from area keeps coverage % roughly constant regardless of render resolution |
| D64 | Neon's sky/grid/sun colours are now drawn from one of 4 fixed, independently-saturated `SYNTH_PRESETS` (indigo top, vivid magenta/pink/orange horizon band, bright cyan or magenta grid) picked by `paletteIdx % 4`, instead of being mixed from the story's own (often pastel) sky/accent/ground colours; a starfield is now always drawn (not gated on `composeScene`'s `tod==='night'`); a solid dark ground-plane gradient was added below the horizon (previously the sky gradient just continued there with nothing distinct); the sun was repositioned to sit low ON the horizon at 1.7x size (was in the upper-sky position `composeScene` gives every other style) | Claude, per coordinator feedback ("not reading as neon... muted olive/brown... only the hero glows") | Mixing a fixed dark colour with an arbitrary story hue can land anywhere, including muddy olive/brown when the story's palette skews warm-green; synthwave is a fixed, recognisable idiom (like watercolour or pixel art already are), so its background now has the same "always vivid regardless of story palette" guarantee pixel art's forced 14-colour palette has, while `paletteIdx` still keeps it seeded/varied story-to-story and the hero itself still uses the story's own colours (per the coordinator: "the hero glow already works") |
| D65 | Environment layers in neon are drawn as bright glow-stroke ridgelines in the grid's own hue (`drawLayerShape`), not filled+dark-stroked like round 2 - only each layer's visible top edge reads (the rest of scene.js's band polygon is off-canvas), giving a horizon silhouette-in-glow instead of the previous dim/muddy fill | Claude, per coordinator feedback ("glowing horizon ridgelines") | Matches the fix's own theme (D64): a filled-and-dimmed shape reads as muddy, a bright stroke reads as a neon tube |
| D66 | Geometric/Bauhaus no longer draws each hero part's own literal ellipse/polygon shape - every part's ROLE picks a PRIMITIVE kind (`circle`/`halfcircle`/`triangle`/`rect`) from a fixed table, and the part's own geometry only supplies where/how big to draw that primitive, oriented outward from the hero's local origin; 1-2 parts (picked by `rng.pick`, same for good/fumbled) get a small deliberate positional offset so the figure reads as overlapping primitives, not a tidy silhouette; the background grid gained a second `multiply` pass over the sky block (the `overlay` pass alone stayed faint on light backgrounds) | Claude, per coordinator feedback ("the hero is just the ordinary silhouette in flat colour... Bauhaus should abstract the hero into bold primitives") | This is the one style where "interpret the shared shape list more strongly in its own idiom" (the cross-cutting round-3 note) meant discarding the literal shape entirely, not just changing how it's filled/outlined (watercolour/papercut/ink/crayon/neon all still trace the part's own silhouette, just in different techniques) |
| D67 | **Round 4**: `js/story/loader.js` never calls `HTMLImageElement.decode()` while `document.hidden` - it waits for a `visibilitychange` to false first (`onceVisible()`), reports the wait through the existing `onProgress(fraction, label)` callback with an honest status string, and retries with a fresh `<img>` (racing `decode()` against the NEXT hide via `onceHidden()`) if the page goes hidden again before a decode settles - `decodeWhenVisible()` in `js/story/loader.js` | Claude, per coordinator's direct finding (`img.decode()` on a blob URL never resolves at all while hidden - probed directly, still pending 6s later vs. 0ms visible) | A player very plausibly taps Play and immediately backgrounds the app; the old unconditional `await warmup.decode()` hung "Painting the story..." forever with no way back. Waiting for real visibility (rather than e.g. swapping in `createImageBitmap`, which weakens the "already decoded, zero-hitch" guarantee stage.js's `<img>` reuse depends on) keeps the guarantee exactly as strong as before, just later |
| D68 | The "page went hidden" status text (`"Paused while you're away - come back to finish loading"`) is sent through `loadStory()`'s existing `onProgress` callback, not a new one | Claude | `js/main.js`'s `setProgress(frac, label)` already renders whatever string `onProgress` gives it verbatim - no change needed on the rhythm-engine agent's side of the ownership split, and the message naturally clears itself (overwritten by the next real step label) the moment loading resumes |
| D69 | **M1.6 round 1**: pixel art gets a global base weight (`BASE_WEIGHTS.pixel = 3`, `js/story/styles/index.js`) multiplied into (not replacing) each minigame's own `stylePrefs` - `weight = baseWeight(id) * (stylePrefs?.[id] ?? 1)` | User, via coordinator ("pixel ≈3x any other style, ~30-35%") | With 7 styles at base weight 1 except pixel at 3, a uniform draw lands pixel at 3/9 = 33.3% exactly inside the target band; multiplying (not overriding) means a minigame that already likes or dislikes another style still applies that preference on top, and pixel's own favouritism is never cancelled out by a style-neutral minigame |
| D70 | `quantize()`'s per-pixel nearest-colour search became a 15-bit RGB lookup table (`buildQuantizeLUT`: 32768-entry `Uint8Array`, 5 bits/channel) built once per distinct palette and cached module-wide (keyed by `` `${paletteIdx}:${mood}` ``, a single-slot cache) so it's built once and reused for every panel of a story that shares a palette | Claude, per coordinator's explicit instruction (measured 3.7-68.6ms naive at various resolutions) | A LUT read is O(1) regardless of resolution or palette size, vs. O(paletteSize) per pixel for the naive search - required *before* raising resolution, since the naive search's cost scales with pixel-count × palette-size, both of which were about to grow |
| D71 | Pixel art's internal frame grew from 112×81/80×58 to **320×~230 normal / 192×~138 low-end** (D per plan section E), and the palette grew from 14 flat colours to **26**, organised as named RAMPS (`buildRamps`: sky/ground/accent/hero/ink/paper, 3-6 shades each, dark→light) instead of a flat unordered list | User ("higher resolution, pixelated yet painterly"), Claude implementation | A flat palette can only ever produce flat colour; ramps are what let a single `colorSlot` (e.g. `'hero'`) resolve to a *range* the renderer can shade across, which flat-list quantization has no concept of |
| D72 | The hero's "volumetric" parts (`body`/`head`/`hull`/`shell` roles only) are filled with a 2-stop `canvas` `createLinearGradient` spanning the FULL ramp (index 0, its darkest shade, to the last index, its lightest) oriented toward the sun/moon (`lightDir`, computed once per render and rotated into the hero's local draw space to stay correct under pose rotation + facing flip); all other parts stay a flat single ramp shade | Claude (revised after visual review - see edge-case table) | A first attempt used only the ramp steps immediately adjacent to the part's own "base" shade (a 3-stop gradient, mid-1/mid/mid+1) and was too subtle to read at hero size once dithered/quantized - spanning the ramp's full dark/light extremes reads as a clear lit-side/shadow-side split instead |
| D73 | The small internal canvas (`small.getContext('2d', { willReadFrequently: true })`) explicitly opts into a CPU-backed (non-GPU-accelerated) 2D context | Claude, bug found via direct profiling | `getImageData()` on a GPU-backed canvas forces a GPU→CPU readback sync that dominated render time at the larger resolution (measured 9-13ms for `getImageData` ALONE at 320×230, vs. ~1.1ms for the LUT-based quantize loop it wraps) - `willReadFrequently` keeps the canvas CPU-backed from the start, since every render does exactly this readback once, by design |
| D74 | Pixel art's declared `cost` changed from 1 to **2** (still `<3`, so still eligible for the low-end style pool) | Claude, per plan section E ("the style's declared cost is re-measured after the change") | Pixel is no longer the cheapest style to render (it's mid-pack after the fixes - see edge-case table for final numbers) even though it's now clearly the most *visually* detailed; `cost` only gates the low-end pool (`<3` stays in, per D34), and keeping pixel out of the cost-3-excluded tier is essential given the user wants it favoured on low-end too |
### Rhythm engine implementation (M1.5 plan steps 1–4, this session)

| # | Decision | Who | Why |
|---|---|---|---|
| D36 | RNG is split into **four independent streams** per seed: `subRng(seed,'chart')` (songform + `game.compose()`), `subRng(seed,'synth')` (backing instrumentation + baked cue soundPacks), `subRng(seed,'sfx')` (live response soundPacks), `subRng(seed,'form')` reserved but currently folded into `'chart'` | Claude, per advisor review | Removing the old baked-melody loop deletes an `rng.int()` draw per note; on a single shared stream that would silently reshuffle every downstream value for the same seed. Independent streams make that a non-event |
| D37 | The learnability lint resolves "is this event predictable" by searching **`chart.cues` itself** for a matching `cueId` ≥1 beat earlier — never a field the composer wrote (e.g. no `predictedByCueTime` on the event) | Claude, per advisor review | A composer bug that mislabels its own predictor would otherwise grade its own homework and pass a broken chart |
| D38 | Every game's "response is predictable" pattern is unified: a priming/tell cue always sits **exactly 1 beat before** what it predicts (Balloon Pump's `pumpTick`/`tapTick`, Swing Samurai's `bonk` fake-tell, Triplet Juggle's `whoosh`/`clap`). Echo Birds and Ready-Set-Go get the same property "for free" because their call-response gap is itself ≥1 beat (or, for Ready-Set-Go's sub-beat gaps `d`, because the *first* cue is `2d` before the response, and `2d≥1` for every allowed `d`) | Claude | One mechanical rule the lint can check, instead of a special case per game. It also produced a real design improvement: fakes get a genuine advance warning (a distinct tell 1 beat early, then silence at the grid slot) rather than an unplayable same-instant reflex test |
| D39 | A hold's **release** deadline is exempt from the "cue ≥1 beat earlier" check; only its press needs one | Claude | The release time is a fixed, already-announced offset from the press (Balloon Pump's whistle rate never changes), audible for the whole hold as the baked `pump` cue itself — not a second discrete event needing its own predictor |
| D40 | Hold length (Balloon Pump) varies 1–4 beats by level, but the whistle's **pitch-rise rate is constant** across every hold | Claude | Reconciles the plan's "the rate never changes" with its "hold lengths 1–4 beats" level knob: the rate, not the length, is the fixed/learnable quantity, and it's continuously audible so a variable length doesn't break predictability |
| D41 | `js/games/echo.js` motif pools are filtered by the **current chart's tempo** (`minGap(motif) * beatDuration >= 0.125s`), and "double chirp" (which halves onset spacing) only ever draws from a separate `doubleSafe` pool whose raw gaps are all ≥1 beat | Claude (bug found via `tools/lint.html`) | 16th-note / doubled motifs that were fine at 90bpm dropped under the lint's 120ms collision minimum at 150bpm; the first `tools/lint.html` run (1686 violations) caught this immediately |
| D42 | A miss plays **no live sfx at all** (not even a soft thud) | Claude, aligning code to the plan's design principle 5 ("hitting feels good and missing is audibly silent") | The pre-rendered `response.tapMiss` buffer still exists in every soundPack (harmless, unused at play time) in case a future screen wants it; `js/main.js`'s `onJudged` just never plays it |
| D43 | Remix (`js/games/remix.js`) is special-cased in `js/songform.js buildChart()`: it calls `pickSubGames()` once per chart, then `composeSection()` (not `compose()`) per section, and every event/cue is tagged with `srcGame` so synth/sfx/mascot all key off the *originating* game, not remix itself | Claude, per plan ("each cue keeps its own sound and mascot") | Keeps remix's implementation additive — no other game module needed to change shape to support it |
| D44 | Remix's occasional 7/8 section (level 8+) is announced by a distinct unscored `oddMeter` cue at the section's start, not by the song's count-in | Claude (deviation from plan wording "announced by the count-in") | The count-in only ever precedes the whole song, not a section 40+ beats in; a per-section announce-cue satisfies "announced before it's played" without inventing a second count-in mechanism |
| D45 | Practice screen reuses the **already-rendered song buffer** (`AudioBufferSourceNode.start(when, offset, duration)` on a slice) and the real `createJudge`/`attachInput` pipeline against that section's own notes, instead of rendering separate practice-only audio | Claude | No extra OfflineAudioContext render; practice sounds and timing are byte-identical to the real game, and holds/fakes "just work" in practice for free |
| D46 | Practice material for cue type N of a game maps directly to that chart's `teachA` (N=0) / `teachB` (N=1) section | Claude | `js/songform.js`'s section-kind sequence already guarantees a `teachA` and (if the game has 2 cue types) `teachB` section exist in every chart — no separate practice-chart generation needed |
| D47 | `js/render/stage.js`'s mascot/cue-icon/pulse elements are children of `.stage__picture`, not `.stage__caption`'s sibling `.stage` container | Claude (bug found via 375px screenshot review) | `bottom:10px` on the mascot was anchoring to the whole flex column (picture + caption), so at narrow widths where the caption wraps to 2 lines the mascot corner overlapped the caption text instead of sitting in the picture's corner |
| D48 | Browser-based verification of "every game at levels 1/5/10 reaches the good/bad ending" was run for all 6 games at level 1, pump/swing/echo at all of 1/5/10 (holds/fakes/tempo-ramp being the highest-risk mechanics), the rest at 1/10, plus one remix run and 2 `?autoplay=miss` runs — not the full 6×3 events matrix | Claude, scope tradeoff | Each run plays out in real AudioContext time (~70–90s, cannot be fast-forwarded) and the pane must stay foregrounded; the learnability lint (4200 charts, <1s) is the exhaustive check, the browser matrix is a sampled realism check on top of it |
| D49 | Backgrounding mid-song (tab hidden, window blurred, or torn down for bfcache/navigation) **pauses** rather than lets the audio clock silently run ahead: `js/pause.js attachAutoPause()` listens to `visibilitychange`+`blur`+`focus`+`pagehide`, `js/main.js` stops the current `AudioBufferSourceNode` (not the whole `AudioContext` - see D50) and any live hold-sustain loops, cancels the rAF loop, detaches input, and shows a "Paused / Tap to resume" overlay | User (via coordinator), prompted by the M1.5 rAF-hidden finding | Without this, `nowCtx >= note.time + MISS_WINDOW` sweeps every note that passed while away into a `'miss'` the player never had a chance at, and `advancePhraseIfNeeded`'s catch-up loop jumps several story panels at once on return |
| D50 | Pause stops the `AudioBufferSourceNode`, not the shared `AudioContext` (`ctx.suspend()`) | Claude | The context is a page-lifetime singleton also used by calibration/menu; suspending it is more invasive for no real benefit here since stopping the source already halts all sound, and capturing `pauseCtxTime = ctx.currentTime` at the pause instant gives everything downstream needs to know "how far into the score had we gotten" without the context ever actually stopping |
| D51 | Resume requires an explicit tap (never auto-resumes on becoming visible again), which reads a fresh `resumeContext()` (satisfies the autoplay-gesture policy if the context ever did suspend for some other reason) and schedules a 4-beat count-in (live `scheduleClick`s, not baked) before a **new** `AudioBufferSourceNode` starts at `newAudioStartCtxTime` offset into the SAME pre-rendered buffer | User (via coordinator) | Matches the plan's existing count-in pattern (M1's intro click track) rather than inventing a second UI convention for "get back on the beat" |
| D52 | Resume point is always a phrase boundary - the phrase that hadn't started yet when paused if there is one, otherwise the next one (`js/songform.js planPauseResume()`) - never mid-phrase | User (via coordinator) | A phrase's cues are what make its notes predictable; resuming into the middle of one drops the player into notes with no cue context, which is exactly what the learnability lint forbids for a normal chart |
| D53 | `planPauseResume(absChart, pauseCtxTime, newAudioStartCtxTime)` is pure data - no DOM/audio - and lives in `js/songform.js`; `js/main.js`'s `pauseNow`/`resumeNow` are the only DOM/audio-touching code, and are thin glue around it plus the already-existing `stage.discardPanel`/`forceCompletePanel` and `input.js attachInput`/`detachInput` | Claude | Directly unit-testable via a plain Node import (see verification below), which is the only reliable way to check this in a harness where the pane's `document.hidden` state makes wall-clock browser verification unreliable (round 2's finding) |
| D54 | Backgrounding is disabled entirely under any `?autoplay=` mode (`pauseNow` early-returns) | Claude | `scheduleEventsAutoplay`'s synthetic pointer-event schedule is built ONCE from the chart's pre-pause note times; a time-shift partway through would desync it from the (now-shifted) chart. Autoplay is a debug/testing tool, real backgrounding is a real-player concern - they don't need to compose, and keeping them separate avoids a whole class of interaction bugs between the two |
| D55 | `js/pause.js`'s `blur` handler is **not** gated on `document.hidden` (only `pagehide` and the initial `visibilitychange` check `document.hidden`) | Claude, prompted by the task's own mobile-Safari note | On mobile Safari, `visibilitychange` can fire late relative to `blur` when switching apps; trusting `blur` unconditionally means the late `visibilitychange` just becomes a no-op re-confirmation. On desktop this also means alt-tabbing away pauses the game, which is correct behaviour for a rhythm game, not a false positive worth filtering out |

### M1.6: playability fixes (calibration, feedback, guides, practice)

| # | Decision | Who | Why |
|---|---|---|---|
| D56 | **THE root-cause fix**: calibration taps are now paired with their **nearest click by time** (`js/calibration.js pairTapsToClicks()`), never by counting position. A tap more than half a beat from every click is rejected outright rather than mis-paired | User (reported a real device playtest: zero hits in every game) | The old `clickTimes[tapCount]` indexing meant one missed/extra/early tap shifted every later pairing by a full beat (600ms @ 100BPM); the resulting offset, applied to every real tap thereafter, pushed all of them outside the ±150ms judging window forever. `?autoplay=` never caught this because it taps at exact chart times, so the same wrong offset cancels out on both sides of the subtraction - only real human input (which never lands on an exact beat multiple) exposed it |
| D57 | Calibration math (`js/calibration.js`) is a **pure module with no DOM/audio** - `pairTapsToClicks`, `computeCalibration` (MAD outlier rejection, ≥6-tap floor, ±200ms clamp-or-reject), `computeVerifyResiduals` | Claude, per plan section F ("verified by direct module import before anything else, since it blocks all real play") | Directly unit-testable via a plain Node import with zero browser dependency - this is what let the fix be verified FIRST, before any other M1.6 work, exactly as instructed. It's also what caught a SECOND real bug (D58) before that ever reached the browser |
| D58 | Self-diagnosis (`ignoredDiagnostics()` in `js/input.js`) always does the **full unconditional nearest-note scan**, never trusting `findPressCandidate`'s own "best so far" as a shortcut hint | Claude (bug found via the new `judging_e2e_test.mjs` regression test, not observed in the wild) | `findPressCandidate`'s search `break`s the instant it sees a note more than `MISS_WINDOW` ahead, WITHOUT first comparing that breaking note's own distance - so exactly at that boundary it can return a much-worse "nearest" note than the true one. Harmless for real gameplay (a genuine tap is always far inside `MISS_WINDOW` of its target long before the break could matter) but wrong for diagnostics, whose whole job is describing a tap that's FAR from where it should be - exactly the case this bug hits |
| D59 | *(±200ms range superseded by D83: the range is now −200…+450ms)* Calibration failure (too few taps, too many outliers, or a measured offset bigger than the ±200ms clamp) **always re-runs**, up to `CAL_MAX_ATTEMPTS` (4); it never stores a guessed/clamped value | User (plan: "clamp to ±200ms; outside that, warn... and re-run") | A device offset that large is far more likely to mean the pairing/attempt went wrong than a genuine reading; storing it anyway would just reproduce the exact zero-hits failure mode this whole milestone exists to fix. After 4 failed attempts it falls back to a neutral 0ms offset (not stuck on the calibrate screen forever) with Recalibrate always one tap away from the menu |
| D60 | Storage version bump (`STORAGE_VERSION = 2`, `storage.migrateIfNeeded()`) wipes calibration AND level/cleared/practicedCues, called once at module load before anything else runs | User's explicit choice (full progress reset alongside the fix) | A returning player's stored offset from before this fix could be a whole-beat-shifted garbage value; keeping their level/cleared progress on top of a broken calibration (invisibly re-corrupted by an old bug) was judged worse than a clean restart. Idempotent - safe to call every boot once already migrated |
| D61 | Ignored-tap events carry `{nearestNote, nearestIndex, deltaMs}` (signed, ms; positive = late) always, not just when convenient | Claude, plan section B | Turns "ignored tap @ 41.2s" into "ignored +412ms LATE (nearest note #12)" - the exact self-diagnosis the zero-hits bug report needed and didn't have. `deltaMs` also feeds the end-of-song median (D62) and the recalibrate suggestion (D63) |
| D62 | The accuracy-independent **median signed offset over ALL taps** (judged + ignored) is tracked separately from the existing perfect/good/miss `debugOffsets` array and shown on both `?debug` and the end screen | Claude, plan section B | The existing `debugOffsets` only ever contained JUDGED taps - in the exact failure mode this milestone fixes (near-100% ignored), that array is empty and shows nothing. The all-taps median is the one number that stays meaningful even when almost everything is being ignored |
| D63 | Recalibrate suggestion triggers at `≥8` same-direction taps (judged OR ignored) with `|median| > 60ms`, shown as a banner + button on the end screen | User (plan's exact threshold) | Turns a systematic miscalibration from "mysteriously bad at every game" into a specific, actionable, one-tap-away fix - the self-correcting loop the original bug report was missing entirely |
| D64 | Guide ticks (section C) are pre-rendered as **two generic (not per-game) buffers** (`renderGuidePack()` in `js/audio/sfx.js`) and scheduled via `AudioBufferSourceNode.start(futureTime)` for every note ONCE at song start (and again, for whatever's left, after a pause/resume time-shift) - never per-frame or per-tap | Claude, plan ("scheduled live... through the existing sfx path... nothing allocated on the tap path") | Generic on purpose: guides are a learning aid layered OVER a game's own cue vocabulary, not one more thing to learn, so they sound the same regardless of which game is playing. Scheduling once up front (not reactively) is what "the existing live-SFX path" and "nothing on the tap path" both actually require - Web Audio's scheduler is sample-accurate regardless of how far ahead `.start()` is called |
| D65 | The visual guide marker is a **pooled ring centered on the picture** that shrinks from 2.3x to 1x scale over one beat and fades in as it approaches - not a per-lane/per-position indicator | Claude | The stage has no lane layout (tap-anywhere design, unchanged since M1) - a single central "approach circle" (osu!-style) is the natural fit, reusing the exact pre-created-pool/transform-only pattern every other stage element (sticker, judgement flash, cue icon) already uses. Holds get a second, dashed-border ring for the release point, sharing the same pool |
| D66 | Fakes are **excluded** from guide targets entirely (`js/render/stage.js setGuideTargets()` skips `n.fake`) | Claude | Telegraphing "something happens here" for a fake would either give away that it's a decoy (defeating the mechanic) or, if the guide ring looked identical to a real note's, actively mislead the player into tapping it. The mascot's bonk tell (already existing, 1 beat ahead per M1.5's D38) remains the only signal for fakes |
| D67 | Guides are enabled by `!storage.getCleared()[game.id]` (on while uncleared, off once cleared), overridable by an explicit `storage.getGuidesPref()` (menu checkbox) or `?guides=on\|off` (checked in that priority order) | User (plan's exact spec) | Matches the existing "practice auto-skips once cleared" pattern (D8) - guides are training wheels for a game you haven't mastered yet, not a permanent crutch, but the player (or a debug link) can always override either way |
| D68 | The corner cue icon shows the **actual per-cue glyph** (`cueTypes[].glyph`, added to all 6 games) instead of a fixed ♪, with a transient name label (`prettifyCueId()` - e.g. `doubleChirp` → "Double Chirp") on the first time THIS SESSION that cueId sounds | User (asked what the three corner indicators mean) | `js/render/stage.js` stays a leaf module (no `js/games/*` import) by taking a `resolveCueType(srcGame, cueId)` callback from `js/main.js` instead of looking cue info up itself - same pattern already used for `resolveGame` (mascot switching in a remix) |
| D69 | The beat-pulse dot's animation was toned down (scale 1.8→1.35, opacity peak 1→0.8), not just relabelled | Claude, plan ("keep the gold dot as a quieter metronome") | Now that the cue icon carries the "something means something, learn it" signal and a guide ring shows "tap now", the pulse dot's only remaining job is a steady, unobtrusive beat reference - it shouldn't visually compete with either of the other two |
| D70 | Practice's pass bar (`≥2 of 3` rounds) judges a round's pass/fail at the same `≥70%` good-or-better ratio the real game clears a LEVEL at, not a separate practice-specific threshold | Claude | One fewer number to explain/tune; "practice uses the same bar as the real thing" is also a more honest signal of readiness than an arbitrarily looser one |
| D71 | Practice has no picture/stage (unlike Play), so "practice uses the same guides as play" is implemented as (a) the identical audio guide-tick buffers, scheduled the same way, and (b) a timeline strip + moving playhead as practice's OWN visual guide, not a literal reuse of `stage.js`'s ring-pool (which needs the picture element practice doesn't have) | Claude (scope/time tradeoff, noted for honesty) | Serves the same purpose (visible "when") through the UI practice actually has; reusing `stage.js` verbatim would have meant giving practice a full picture panel it doesn't otherwise need |
| D72 | `calibration_test.mjs`, `judging_e2e_test.mjs` and `pause_test.mjs` live in the repo as `tests/*.mjs` (plain ES modules, relative `../js/...` imports, no framework/dependency), with `node tests/run.mjs` as the one command that runs all three and exits nonzero on any failure | Coordinator (round-2 follow-up) - these had only existed in the session scratchpad, which is discarded at session end | These are the tests that catch the exact bug class that made the game unplayable (calibration pairing, judging at known offsets, pause/resume time-shift correctness) - they need to outlive the session that wrote them. `node tests/run.mjs` documented in README.md's new Testing section |
| D73 | `?debug` overlay moved from bottom-left to **top-right**, shrunk (`max-height: 32vh`), and given `pointer-events: none` | Coordinator (found it covering the end screen's "Play again" button) | Every screen's interactive buttons live in a centered panel or a bottom `.menu-actions` row - top-right never sits over them on any screen. `pointer-events: none` is belt-and-braces so it structurally can't block a click regardless of viewport size, since the debug log only ever needs to be read (rows auto-prepend, capped at 40), not scrolled by hand |

### Engineering

| # | Decision | Who | Why |
|---|---|---|---|
| D18 | `AudioContext.currentTime` is the **only** clock. Taps are timed from `event.timeStamp` on `pointerdown`, mapped via `getOutputTimestamp()`. Never `Date.now()`, rAF timestamps, or handler run time | Claude | Wall-clock and frame times drift from audio; on phones this breaks judging entirely |
| D19 | `outputLatency` is subtracted **only** on the fallback path, because `getOutputTimestamp().contextTime` is already the audible time | Claude (bug found in review) | The original code subtracted it twice, making every tap look ~100–300 ms early on phones/Bluetooth |
| D20 | A **calibration screen** (metronome, 12 taps, median offset in localStorage) runs on first use, and doubles as the gesture that resumes the AudioContext | Claude | Output latency varies hugely per device; it also satisfies browser autoplay rules |
| D21 | Judgement windows: Perfect ±45 ms, Good ±90 ms, auto-miss 150 ms after the note; stray taps are ignored leniently | Claude | Standard-ish rhythm game feel; leniency avoids punishing enthusiasm |
| D22 | All panels are **rendered and decoded before play**, behind a loading screen. Nothing is fetched or decoded during play | Claude | "Zero delay when tapping" — a downloaded-but-undecoded image still hitches on first paint |
| D23 | At tap time only `opacity`/`transform` change on pre-created DOM; no allocation, no `drawImage`, no layout | Claude (fixed in review) | The first implementation built a 1280px canvas per tap, which risked hitches on low-end devices |
| D24 | Each panel keeps **two decoded images** (good, fumbled) as object URLs, with per-region `<img>` elements sharing them via CSS band masks | Claude (fixed in review) | The cumulative-region design used ~75 MB per panel *and* let one miss fumble the whole picture |
| D25 | Canvas size cap: longest edge 1280 px (720 px in low-end mode), DPR capped at 2, counting DPR in the cap | Claude | Low-end memory; a DPR bug had been producing 1920px canvases |
| D26 | Seeded RNG (mulberry32) with separate streams per subsystem; `?seed=` reproduces a whole game | Claude | Makes bugs reproducible and lets a player replay a game they liked |
| D27 | Beat detection for uploaded/online audio is **classic DSP in a Web Worker** (spectral flux → autocorrelation → dynamic-programming beat phase), not an AI model, with a manual BPM/offset nudge | Claude | The user asked "probably by AI?"; DSP needs no download, no server and no added latency, and procedural songs need no detection at all since the chart generates the music |
| D28 | Implementation is delegated to Sonnet subagents with **split file ownership** (rhythm engine vs art styles), reviewed by Opus | User (Opus designs, Sonnet codes) | Parallel work without edit conflicts |

| D75 | Repo name: **`openrhythm-tapestry`** | User | Picked from Claude's suggestions |
| D76 | Product name **TapESTORY** replaces "OpenRhythmOD" in every user-visible title and heading (`index.html`, `tools/*.html`, manifest, README/docs/CLAUDE.md headings). Left unchanged: the folder path `...\OpenRhythmOD` (the user's to rename), immutable `docs/sessions/*` logs, and the repo name (D75). No storage keys contained the old name, so saved progress is unaffected | User | Branding. The repo description is "TapESTORY: tap to the beat and weave instant mini art-stories..." |
| D77 | **Offline support via a service worker** (`sw.js` at the repo root). On install it precaches every asset into `tapestry-<VERSION>`, then calls `skipWaiting`. On activate it deletes other `tapestry-*` caches, then calls `clients.claim`. Fetch is cache-first for same-origin GETs, and navigations use `ignoreSearch` | User asked; Claude designed | Page-open play already worked offline. This also makes reloads and later visits work offline, and it stays within D5 |
| D78 | `sw.js`'s `VERSION` is a **content hash** and `ASSETS` is generated. Both are written by `node tools/stamp-sw.mjs`, a hand-run maintenance helper, not a build step. `tests/sw_test.mjs` fails if either is stale. **Run the stamp script before every deploy** | Claude | Changing the file bytes is how browsers detect a new worker. A hand-bumped version is easy to forget, and the test turns forgetting into a red test |
| D79 | The worker is **not registered on localhost / 127.0.0.1 / [::1]** unless `?sw=on`. `?sw=off` unregisters it | Claude | A cache-first worker on the dev server would serve stale modules and break the edit→verify loop (see the ES-module-cache edge case) |
| D80 | Web app manifest + `icon.svg` (SVG only, `sizes: "any"`), so the site can be installed as an app | Claude | "Visit once, play offline" pairs naturally with home-screen install. PNG icons (192/512, iOS apple-touch) are a possible follow-up if an installer demands raster icons |
| D81 | Implemented by a Sonnet subagent and reviewed by Opus, per the new global "Opus orchestrates, Sonnet codes" rule. `sw.js`/`stamp-sw.mjs` were drafted by Opus just before that rule landed; Sonnet reviewed them and kept them unchanged | User (global rule) | — |
| D82 | **M1.7 calibration:** 4 count-in clicks with an on-screen 4·3·2·1 countdown before the 12 recorded clicks (the verify pass gets 2). Taps before `firstRecorded − 0.3·beat` are ignored **by time** (`isCountInTap`). The "Start tapping" button was removed, so the countdown starts on open. A **Back** button cancels: a flag checked after every await, a per-run `GainNode` disconnected to silence scheduled clicks, and nothing saved. The status says to tap along with the *sound*, with no visual pulse | User (count-in, no Start button, a way out), Claude (the mechanics) | The first taps were always off. With the count-in, a separate Start button is redundant. Visuals lead the audio on Bluetooth, so they would bias the measurement |
| D83 | `CAL_BPM` 100→**80**. **Asymmetric pairing**: a tap belongs to click *c* only if −0.3·beat ≤ t−c < +0.7·beat. The accepted range is **−200…+450ms** (`MIN_OFFSET_MS`/`MAX_OFFSET_MS`, replacing `CLAMP_MS=200`). An offset above 150ms shows "Bluetooth audio adds some delay - we've compensated." | Claude, from the user's Bluetooth report | Bluetooth adds ~150–350ms that `getOutputTimestamp()` often doesn't report. Yesterday's ±200ms clamp (D59) rejected these genuine readings, and before M1.6 any value was stored, which is why it had worked on earlier days. Output latency only ever makes taps late, so the pairing window leans late |
| D84 | **Every time-based decision uses the "heard" clock** `now − calibrationOffsetSec`: `createJudge.update()`'s auto-miss sweep (`js/input.js`), `stage.tick` visuals (guide rings, cue icon, mascot, beat pulse), `advancePhraseIfNeeded`, the song-end check, and the practice playhead and end. `pauseNow` ignores a pause arriving after the raw end | Claude (Sonnet found the input.js half while implementing; Opus asked for the phrase-advance and song-end half in review) | `judgeTap` already subtracted the offset but `update()` didn't, so on a +250ms device every note auto-missed before the late tap arrived. Calibration alone would have left Bluetooth players scoring ~0. Visuals drawn at the scheduled time also ran ~offset ahead of the sound |
| D85 | **Practice teaches both halves of call-and-response.** The timeline shows cue markers (hollow, with the cue glyph, "listen"), tap markers (filled, "tap", which turn --hit green or --miss red) and fake markers (✕, "don't tap"; the legend entry appears only when the section has a fake). Both Listen and Your turn get a visible 4·3·2·1 count-in. The Listen demo **plays the answer** (tapGood, and holdSustain→holdRelease for holds, like Play). Hits in Your turn play the response sound, and misses are silent. All practice audio goes through a per-phase bus that is disconnected when the phase stops. "Hear it again" is disabled during a scored round | User (Echo Birds: 3 dots but 4 clicks), Claude | The 4 clicks were the unmarked count-in and the chirps had no markers, so there was no way to tell listen beats from tap beats. The demo never played the response (D16), so it didn't show what "correct" sounds like |
| D87 | The README gained a **"Deploying your own copy"** section: clone and run, Node ≥20 needed only for the tests and stamping, `node tools/stamp-sw.mjs` + `node tests/run.mjs` after any change to a file the game loads, commit the stamped `sw.js`, and GitHub Pages settings. The Calibration section was updated for M1.7 (80 BPM, count-in, −200…+450ms, Back) | User asked; Claude wrote it | Anyone cloning the repo needs to know that skipping the stamp leaves returning players on the old version forever |
| D88 | New **`.gitignore`**: `.claude/settings.local.json` (per-machine Claude permission rules), `graphify-out/`, `node_modules/`, OS clutter and `commit-msg.txt`. `.claude/launch.json` stays tracked (the shared dev-server config). `settings.local.json` was already in the initial commit, so the user must also run `git rm --cached .claude/settings.local.json` | User asked; Claude chose the entries | Local permission rules don't belong in a public repo; the derived graph and scratch files are regenerated or temporary |
| D86 | **End-screen fix:** restored `#end-seed`. `showEndScreen` now assigns the button handlers **before** any DOM writes. New `tests/dom_ids_test.mjs` checks that every `getElementById('…')` in `js/` exists in `index.html` | Claude (reproduced the user's report) | The missing element threw before the handlers were attached, so Play again / Replay / Back to menu did nothing. The existing tests couldn't catch a DOM/HTML mismatch |

### Accounts & cross-device progress (section C, this session)

| # | Decision | Who | Why |
|---|---|---|---|
| D89 | **Corrected default sign-in flow**: passwordless 6-digit email code (`POST /auth/v1/otp {email, create_user:true}` → `POST /auth/v1/verify {type:"email", email, token}`) is the default, with "Use a password instead" (`/auth/v1/signup`, `/auth/v1/token?grant_type=password`) as the alternate, and `PUT /auth/v1/user {password}` as a "Set a password" action for already-signed-in players | User (explicit correction to the plan's headline) | Matches Supabase's actual OTP flow; the plan's own body text already described this correctly, only its headline line needed fixing |
| D90 | New files only: `js/config.js`, `js/net/auth.js`, `js/net/sync.js`, `js/net/merge.js`, `js/net/accountUi.js`. `js/main.js` gets exactly 5 small additive touch points: one import, two `screens{}` map entries (`account`, `progress`), one `if (name==='menu') accountUi.onMenuShown()` line inside the existing `showScreen()`, one `accountUi.init({showScreen})` call at boot, and one `accountUi.recordPlay(...)` call in `finishPlay()` right before `showEndScreen(...)` | Claude, per the task's file-ownership split | A rhythm-engine Sonnet agent was working the same `js/main.js`/`index.html`/`css/game.css` in parallel on practice/end-screen/miss-breakdown work (plan section A/B); keeping every edit additive and re-reading each file immediately before editing it (per instruction) minimises the chance either agent's diff clobbers the other's |
| D91 | The session (`{accessToken, refreshToken, expiresAtMs, userId, email}`) is stored via **new `js/storage.js` exports** (`getSession`/`setSession`/`clearSession`), not inside `js/net/auth.js` itself | Claude | Keeps the existing project convention that ALL localStorage access goes through `js/storage.js` (`safeGet`/`safeSet`, try/catch degrade-gracefully) - `js/net/auth.js` stays fetch-only glue |
| D92 | `js/net/merge.js` owns the **outbox** enqueue/dedupe/flush bookkeeping as pure functions (`buildOutboxRow`, `enqueueOutbox`, `applyFlushResult`), not just `mergeProgress` - `js/storage.js`'s `enqueuePlay()` and `js/net/sync.js`'s `flushOutbox()` are thin wrappers that just add the one bit of real impurity (`crypto.randomUUID()`) and the localStorage/fetch calls | Claude, per the task's own description of `merge.js` as "pure: the progress merge + outbox logic, Node-testable" | Lets `tests/merge_test.mjs` exercise the full enqueue → failed-flush → successful-flush → enqueue-again lifecycle with a **stubbed transport** (a plain boolean standing in for a real fetch result) and zero localStorage/fetch/DOM, mirroring `js/calibration.js`'s existing pure-math pattern |
| D93 | `pushPlays()` (`js/net/sync.js`) upserts with **`Prefer: resolution=ignore-duplicates`**, not `merge-duplicates`; `pushProgress()` correctly keeps `merge-duplicates` | Claude (deviation from the plan's one-line "upsert via `Prefer: resolution=merge-duplicates`", which only fits `progress`) | `plays`' RLS has no UPDATE policy (append-only: insert+select only, per the plan's own data-model section) - a `merge-duplicates` upsert's implicit `UPDATE ... ON CONFLICT` would be rejected by RLS on every retried flush, silently breaking the exact "retried flush never double-inserts" guarantee the plan asks for. `DO NOTHING` (`ignore-duplicates`) never attempts a write on conflict, so it works under an insert-only policy; `progress` **does** allow update, so `merge-duplicates` is correct there |
| D94 | The menu account chip is a **name button** (`Guest · Sign in` when signed out, `{name} · My progress` when signed in) plus a **separate one-click Sign out button** (shown only when signed in), rather than literally rendering "Guest · Sign in" / "name · Sign out" as one static chip | Claude (reasonable reading of the plan's illustrative chip text) | The plan names a required "My progress" screen but never says what opens it - making the signed-in chip's primary action navigate there (with sign-out as a clearly-separate quick action, also duplicated on the My progress screen itself) is the natural entry point |
| D95 | The display-name prompt (`#account-step-name`) is shown **once**, right after a successful sign-in, only if `sync.pullProfile(userId)` comes back with no `display_name` set; a network failure while checking just skips the prompt (proceeds straight to the menu) rather than blocking sign-in | Claude | "Prompt once" per the plan; failing open (skip rather than retry-loop) keeps a flaky connection from ever trapping a player mid-sign-in - the prompt simply reappears next sign-in if it never got answered |
| D96 | `js/net/sync.js` is the **only** module that calls the Supabase REST API, and it is only ever invoked from `js/net/accountUi.js`'s menu/end/account/progress-screen code paths (`onMenuShown`, `recordPlay`, `openProgressScreen`, the sign-in/password/set-password handlers) - never from anything reachable during Play | Claude, per D11 | Directly auditable: grep `js/net/sync.js` callers and every one is a screen-transition or end-of-run hook, never inside `startPlay()`'s frame loop |
| D98 | **Direction: Rhythm-Heaven hybrid.** Each minigame gets an animated character stage in front of the painted story panel (characters perform cues and react to hits and misses), with authored song templates varied by seed and a Try again / OK / Superb result next to the good/bad ending painting. It is prototyped on **Echo Birds only**, and the user reviews it before the other 5 games are converted | User (chose the hybrid), Claude (design) | The user's playtest (median timing −42…+11ms, so calibration is fine) showed the misses come from rhythms being hard to read and from indirect feedback, not from timing. A full clone would drop the art-style/story identity and endless procedural content |
| D99 | **Accounts: Supabase over plain `fetch`, passwordless 6-digit email code by default**, with "Use a password instead" and "Set a password" as options. Chosen over a Cloudflare Worker, which would mean writing auth ourselves (PBKDF2-only hashing within a 10ms CPU limit, sessions, rate limits) | User (code-first default and password option), Claude (Supabase recommendation) | This is a course demo: managed, bcrypt-hashed auth plus RLS is safer and demonstrable in the table editor. Free-tier catches: the project pauses after ~7 days idle, and the built-in email sender allows only a few emails per hour (a custom SMTP sender or a pre-made password test account covers the demo) |
| D100 | **Gentler levels 1–2:** at level 1 every game uses one cue type, only whole-beat responses, ≤90bpm and teaching sections +4 beats. Level 2 adds exactly one variation (Swing Samurai's swing and Triplet Juggle's triplets move to level 2). Levels 3+ are unchanged | Claude, from the user's "sweet spot of the first simple version" feedback | Enforced by `tests/levels_test.mjs`. The lint sweep is still 0 violations across 4800 charts |
| D101 | **Practice retry:** after the rounds end, Try again / Continue / Skip are shown, and "Hear it again" chains into a scored round. A round with 0 judged notes is a **fail** (pure `js/practice.js practiceRoundOutcome`). A round the tab was backgrounded during is **interrupted**: it's not counted, the screen shows "Paused - come back to continue", and the round re-runs once the page is visible and focused | User (Hear it again was a dead end), Claude (the outcome rules) | Browser review found "2 passed" with zero taps, because no rAF means no auto-miss. The first fix re-ran interrupted rounds immediately, which would loop audio forever in a hidden tab |
| D102 | **End-screen miss breakdown:** no tap / too early / too late, plus misses per cue type (worst first, e.g. "♪ Chirp: 34 of 34 missed"), with counters only during play and strings built at song end | Claude | It shows *which* rhythms are unreadable, which is the data the Echo Birds prototype needs |
| D97 | `js/net/auth.js`'s `authorizedFetch()` calls `ensureFreshSession()` first (proactive refresh within 60s of expiry, `REFRESH_MARGIN_MS`), then retries the request exactly once more on a `401` (covers a token that expired mid-request or was revoked) | Claude, per the plan's "refresh...on a 401, retry once" | Two independent triggers for the same refresh path keeps the common case cheap (one proactive check, no wasted round trip) while still covering the rarer race |

### Tooling (machine-level, not part of the game; 2026-09-23 session)

| # | Decision | Who | Why |
|---|---|---|---|
| T1 | Install the `supercharge` skill (github.com/LessComplexity/supercharge) into the **global** `~/.claude/skills/supercharge`, adapted for Windows | User | The user asked for it. Upstream `install.sh` is POSIX-only |
| T2 | An earlier install that was byte-identical to upstream `main` existed as `~/.claude/skills/supercharge_`, and was **renamed** (`Move-Item`, not copied) to `supercharge` | Claude | `/supercharge-start` and `/supercharge-end` (already in `~/.claude/commands`, identical to upstream) invoke `skill: supercharge`, which could not resolve under the `_` name. Renaming instead of copying avoids two skills with the same frontmatter name. No settings.json entry referenced it, so it wasn't a deliberate disable |
| T3 | `scripts/preflight.sh` and `scripts/drift-check.sh` were ported to **pure-ASCII PowerShell 5.1** (`preflight.ps1`, `drift-check.ps1`), placed next to the originals, and SKILL.md gained one marked "Windows (local addition)" section | Claude | `bash.exe` on this machine is only the WSL launcher, and even with a distro it wouldn't see Windows-side tools. ASCII-only because PS 5.1 misreads non-ASCII in BOM-less UTF-8 scripts. User PATH and execution policy were left unchanged (`-ExecutionPolicy Bypass -File` per call) |
| T4 | Dependencies: OpenSpec 1.13.1 was already present via npm, and its telemetry was set to off (matching upstream installer behaviour). **Not installed:** git (drift-check hard-requires it), uv → semble/graphify, and Bun → gbrain (which also needs an interactive `gbrain init` with an API key) | Claude | These are system-level installs or piped remote install scripts, so they're left for the user to confirm |
| T6 | `drift-check.ps1` and `preflight.ps1` fall back to **WSL's git** (`wsl.exe -e git`) when Windows has no git. This supersedes T4's "git not installed" for drift checking | User (git and Python live in WSL; C: is `/mnt/c`), Claude implementation | Avoids a second git install on Windows |
| T7 | Claude must **never git add/commit/push** to the user's (public) repos. The user does that. Temp-dir self-test repos (created and deleted by `--selftest`) are fine | User | Explicit instruction |
| T8 | semble 0.5.5 and graphify 0.9.66 are installed **in WSL** via `uv tool install` (the user installed uv with `pipx`). `preflight.ps1` detects WSL tools, and SKILL.md says to call them via `wsl -e bash -lc`. This supersedes T4 for these two tools. gbrain is still not installed | User (chose WSL, installed uv), Claude | Auto mode blocked piping the astral.sh installer, and WSL Python had no pip/venv, so the user installed uv via apt/pipx |
| T9 | The "WSL git/Python" and "never add/commit/push" rules moved to the **global** `~/.claude/CLAUDE.md`. They supersede the project-memory note, which is removed from the index | User | These apply to every project |
| T10 | semble and graphify are registered as **user-scope MCP servers** in `~/.claude.json` (`command: wsl.exe`, `args: [-e, bash, -lc, semble \| graphify-mcp]`), merged with WSL python3. The backup is `~/.claude.json.bak-supercharge`. This supersedes T5's "plugin only" for semble | User | Both tools live in WSL. wsl.exe inherits the project cwd (verified: `/mnt/c/FMW/Code/Claude/OpenRhythmOD`), and a login shell is what puts `~/.local/bin` on PATH |
| T11 | graphify was reinstalled as `graphifyy[mcp]` | Claude (bug found by stdio smoke test) | The plain `graphifyy` install's `graphify-mcp` crashed with `ModuleNotFoundError: mcp`. After the fix, both servers answer an MCP `initialize` request |
| T12 | The stale project memory `wsl-git-no-commits.md` was deleted | User | Superseded by the global CLAUDE.md (T9) |
| T5 | Not copied to `~/.copilot/skills` (upstream would, since that folder exists). No AGENTS.md or Kimi MCP steps (none of those folders exist). The semble MCP server is only wired via `/plugin install supercharge@supercharge`, which would duplicate the bare skill, so use one or the other | Claude | The user asked for the global Claude skills folder only |

---

## Edge cases

| Case | Handling | Covered by |
|---|---|---|
| Browser blocks audio until a user gesture | The AudioContext is created suspended and resumed by the calibration/Start button | Any real playthrough |
| Device output latency varies (Bluetooth, low-end Android) | Calibration offset stored per device in localStorage, applied at the judging site only | `?autoplay=events` offset stats (≈ −55 ms uncalibrated, flat over a song) |
| `getOutputTimestamp()` missing | Falls back to `currentTime` minus `outputLatency`/`baseLatency` | Code path in `js/clock.js` |
| localStorage unavailable (private mode, blocked) | Wrapped in try/catch; the game runs, and calibration just isn't remembered | `js/storage.js` |
| Browser tab/pane hidden mid-song | Timers and rAF throttle while the audio clock keeps running; the game catches up, and a phrase jumped over is skipped cleanly rather than shown | Bug found and fixed (multiple `showPanel()` calls in one tick revoked blob URLs in use) |
| Player stops tapping mid-phrase | Unjudged notes auto-miss, and `forceCompletePanel` reveals the rest of the picture at the phrase boundary | `?autoplay=miss` run |
| A miss inside an otherwise good phrase | Only that region shows the fumbled art; neighbouring regions keep their own variant | `?autoplay=perfect&missEvery=3` run |
| Taps with no note nearby | Ignored (lenient); `?debug` distinguishes "ignored" from "judged" | Debug overlay |
| Two judgements in quick succession | The judgement text pool has 2 elements so "Perfect!" and "Miss" don't overlap | M1.5 polish |
| Held key auto-repeat / pointercancel / lost focus | Key repeat ignored; `pointercancel` and focus loss count as a release | M1.5 hold input |
| Fake-out cues ("don't tap") | A tap within ±120 ms of a fake = Miss; correctly ignoring one counts as a hit | Learnability lint + autoplay |
| Story has fewer panels than taps | Each tap paints in the next region of the current panel; the panel advances at the phrase boundary | Core reveal design |
| Song shorter than intended at high BPM | Phrase count adapts in M1.5's song form (M1 had 35–40 s outliers) | M1.5 song form |
| Low-end device (`deviceMemory ≤ 2`, `hardwareConcurrency ≤ 4`, or `?lowend`) | Smaller canvases, fewer paint layers, 22.05 kHz render, cheaper art styles | `?lowend` load timing |
| Offline / API failure in M3 | Online sources are optional and fall back to procedural content | M3 |
| CC-BY assets | Attribution is always shown on the end screen, and stored in pack manifests | M3 |
| Paper cut-out torn-edge amplitude scaled with segment length (`off = jag * segLen/teeth`) instead of being an absolute pixel offset | Fixed: `deckleEdge()` now applies `jag` directly as the max px displacement; background/hero/prop/sun jag values tuned to 3-9px | Bug found via `tools/styles.html` — hero silhouettes rendered as unrecognisable spiky stars before the fix |
| Geometric/Bauhaus and Neon fumble transforms picked a "fallen"/"broken" part uniformly at random, including `dot` (eye) parts that have no code path to visibly fall/break | Both styles now restrict the random pick to parts whose shape actually supports the effect (`ellipse`/`polygon` for fallen, everything but `dot` for broken) | Bug found via multi-hero/multi-seed screenshots — some fumbled renders looked identical to the good one |
| Neon spark-burst length was a fixed pixel range, so on a small canvas (relative to hero size) the sparks visually swallowed the hero | `paintSparks()` now takes a `scale` (heroSize) and sizes sparks/blur/line-width relative to it | Bug found by rendering the same style at multiple canvas sizes |
| Geometric/Bauhaus background grid was invisible (drawn before the opaque sky/ground colour blocks, so fully covered) | Grid now drawn after the colour blocks with `globalCompositeOperation: 'overlay'` so it reads on both light and dark blocks | Bug found via screenshot review |
| `tools/styles.html` used `requestAnimationFrame` to yield between renders; rAF never fires while the tab is backgrounded/hidden (true for this project's automated browser-pane workflow), so the whole contact sheet hung indefinitely | Yield helper switched to `setTimeout(fn, 0)`, which still runs (if throttled) in a hidden tab | Bug found when the sheet stalled at "Rendering Watercolour (1/7)" under the automated browser pane |
| Two agents (art styles vs. rhythm engine) driving the **same shared browser pane** in parallel can navigate/close each other's tabs mid-script | No code fix (environment constraint); mitigate by opening a dedicated tab per agent and treating any unexpected `document.title`/URL after an `await` as a sign the tab was reused elsewhere | Observed directly: a `loadStory()` test's result looked wrong because the tab had been navigated to the other agent's `tools/lint.html` mid-script |
| A hold's own release deadline can occur **after** a later note's own press time (holds aren't contiguous in the notes array, which is sorted by press time) | `createJudge.update()` no longer early-`break`s its sweep loop once one not-yet-due note is seen; it scans the whole remaining range every frame (cheap at this chart size) | `?autoplay=events` on Balloon Pump at levels 1/5/10 with mixed taps-between-holds |
| Player taps near a fake, vs. correctly not tapping it | Tap within ±120ms of a fake's grid slot = Miss (the "bonk" penalty); nothing tapped there within the window = `judgement:'avoided'` (a success, scored like Good, revealed as the panel's good variant, but silent — no live sfx, since the player didn't actively do anything) | Swing Samurai `?autoplay=events` (fakes always skipped → `Avoided` count matches the level's fake count exactly) and `?autoplay=miss` (fakes still resolve `avoided`, but the fake rate is capped well under the 70% pass threshold so `?autoplay=miss` still reaches the bad ending) |
| `?game=`/`?level=` debug overrides must not corrupt the player's real saved progress | `applyProgression()` no-ops entirely (no `storage.setLevel`/`markCleared`/`incrementGamesPlayed`) whenever either URL param is present | Forced run's end screen showed "next up: level 1" (unchanged) after a `?level=1` win, while an unforced run correctly advanced `ord.level` |
| Remix with fewer than 2 cleared games | Gated one level up: `js/games/index.js isRemixTurn()` only offers remix once `clearedIds.length >= 2` during normal ladder progression. But a caller that skips that gate (`?game=remix` on a fresh profile, or any other direct `buildChart(rng, REMIX, level)` with `<2` cleared) needs a real song too, not silence: `remix.pickSubGames()` **falls back to picking 2-3 games at random from all 6** rather than returning a too-small (or empty) pool | **Round 2 bug, fixed**: `buildChart(rng, REMIX, level)` with 0 cleared games previously returned a chart with **zero notes** - a silent, unplayable song, hit directly by `?game=remix` in a fresh browser profile. `tools/lint.html` now runs a dedicated "Remix (fresh profile, no cleared games)" pass (0 violations, every chart non-empty) in addition to its normal all-6-cleared pass |
| Tap density varies a lot by mechanic (Bouncy Ball/Ready-Set-Go score ~1 judged event per multi-beat cue cycle; Triplet Juggle scores 3 per whoosh) | Reduced fixed inter-cycle rest to a mostly-sub-beat, sometimes-zero amount in `bounce.js`/`ready.js`/`pump.js` (and fixed 3 places where the "does the next cycle fit" check demanded a full extra beat of headroom beyond what was actually needed, which had been silently capping every section at 1-2 cycles); shifted Balloon Pump's hold:tap mix from 70:30 to 55:45; gave Triplet Juggle a level-scaled chance of an extra rest beat between runs and leaned its call mix a bit more toward the sparser single-clap | Round 2: coordinator's direct-import measurement (525 charts) flagged bounce ~19-29 and ready ~23-34 notes/song against swing/echo/triplet's 32-88; re-measured after the fix (25 seeds × levels 1/5/10): bounce 19.4→24.1/24.2→30.1/29.0→37.1, ready 23.0→29.8/26.8→34.7/32.8→42.4, pump 26.1→28.4/32.1→36.2/31.7→37.3, triplet (reduced) 61.8→51.2/72.7→60.8/87.0→69.3 avg notes at L1/L5/L10 - closer to the "roughly 35-60" band across every game without changing any mechanic's core readability (teaching sections keep their full rest throughout) |
| Player backgrounds the app mid-song (switches apps, locks the phone, alt-tabs) | `js/pause.js` + `js/main.js` pause on the first `visibilitychange`/`blur`/`pagehide` signal (whichever fires first - see D55), show "Paused / Tap to resume", and freeze judging entirely (rAF loop cancelled, input detached, source node + any hold-sustain loops stopped). Resume requires a tap, plays a live 4-beat count-in, and restarts a NEW `AudioBufferSourceNode` at the start of the next unstarted phrase - never mid-phrase - with every note/cue/phrase from that point on time-shifted to match (`js/songform.js planPauseResume()`) | Round 3. Verified via a 72-assertion direct Node import of `planPauseResume` (6 games × 3 levels × 4 cases: pause-before-a-phrase, pause-mid-phrase, pause-in-the-outro, already-judged notes never overwritten) - this caught a real off-by-one (a very-late pause computed `resumeIdx = phrases.length + 1`, one past the valid "resume in the outro" sentinel) before it reached the browser. `js/pause.js`'s listener/dedup logic was separately verified via a synthetic `visibilitychange`/`blur`/`focus`/`pagehide` dispatch sequence in a live tab (document.hidden mocked, since it's read-only) - trigger-once-per-away-spell and detach() both behaved exactly as scripted. A live run confirmed the whole cycle for real: started Bouncy Ball level 1, dispatched a real `blur` mid-song, saw it actually pause (`?debug` log + the overlay's text present in the rendered page), tapped resume, and saw it correctly compute and report "skipped 7 note(s), restarting at phrase 3" with no console errors. (Continuing to the end screen after that hit the same pane-`document.hidden`/rAF-suspension issue as round 2, and a separate run also hit a transient ~1-minute loading-screen stall that cleared on retry - both pre-existing environment quirks, not regressions from this change.) |
| Notes that passed while backgrounded must not be judged as misses | Marked `judgement:'skipped'` directly (not routed through `js/input.js createJudge`'s normal resolution path, since the player never got a chance to act) - excluded from both the accuracy numerator AND denominator in `js/main.js finishPlay()`, and their story region is revealed via the same `stage.forceCompletePanel()`/`discardPanel()` calls already used for "the player stopped tapping" at song end, so the picture stays coherent either way | `counts.skipped` shown on the end screen only when nonzero; verified structurally via the Node test above (a note marked 'skipped' is never counted in `judged`/`accuracy`) |
| **M1.6 root cause**: calibration paired the n-th tap with the n-th click by COUNTING, not by time - one missed/extra/early tap during the 12-tap calibration round shifted every later pairing by a full beat, and the resulting garbage offset silently made every real tap in every game land outside the ±150ms window forever | `js/calibration.js pairTapsToClicks()` now matches each tap to whichever click it's actually closest to in time (rejecting anything >half a beat from every click outright), with MAD outlier rejection, a ≥6-valid-tap floor, and a ±200ms clamp-or-reject on the result - see D56-D60 | Reported via a real device playtest (zero hits, every tap "ignored"). Regression-tested directly: a synthetic "missed first tap" scenario (the exact bug trigger) recovers the true offset instead of a ~beat-shifted one; `?autoplay=` never caught this originally because it applies the SAME (wrong) offset on both sides of the tap-time subtraction, so the error cancels out - only real, non-grid-aligned human input exposes it |
| A tap far outside the normal judging window (a real, in-the-wild miscalibration, not the ~1-frame stray taps the old lenient-ignore path was designed for) needs a USEFUL diagnostic, not just "ignored" | Every ignored tap now carries `{nearestNote, nearestIndex, deltaMs}` (signed ms, always computed via a full unconditional nearest-note scan - see D58) - `?debug` rows read like `ignored +412ms LATE (nearest note #12)`, and the end screen shows the median signed offset over ALL taps (judged + ignored), with a "Your taps are consistently Nms late - recalibrate?" prompt once ≥8 taps agree in direction and \|median\| > 60ms | Section B, verified two ways: a direct `createJudge` test with taps at exactly ±600ms from sparse synthetic notes (confirms the ignored-event deltaMs reads ~600ms, not just "ignored"); and, along the way, this test caught D58 (the diagnostic scan trusting a bad hint from `findPressCandidate` right at its own search boundary) |
| Guides (section C) must reflect a pause/resume's time-shift without any extra bookkeeping | `stage.js`'s guide-ring pool stores `{note, isRelease}` referencing the LIVE note objects (not a time snapshot) - `planPauseResume()` mutates those same objects' `.time`/`.releaseTime` in place, so the guide system stays correct across a resume automatically. Guide TICK SOUNDS do need an explicit re-schedule (`scheduleGuideTicks()` called again in `resumeNow()`, after the shift), since a buffer once scheduled can't be "moved" - `n.judged` (true for both normally-judged and pause-skipped notes) is what keeps it from double-scheduling already-resolved notes | Code-reviewed carefully (not exercised live end-to-end - see the M1.6 session-status note below for why); the underlying `planPauseResume` mutation behavior itself is covered by the round-3 pause regression test, which this design deliberately depends on rather than re-deriving |
| **Round 2**: crayon's background `crayonFill` (sky/ground/environment layers) used `strokeLen = max(box.w, box.h) * 0.3` - fine for a roughly-square hero part, but for a wide-and-shallow box (the sky strip) this produced strokes as long as the box was *wide*, which piled up into big opaque diagonal bars burying the hero | `strokeLen` now uses `min(box.w, box.h, 160) * 0.32` (short dabs regardless of box aspect ratio), plus background-layer stroke counts were cut further (sky ×0.7, environment layers ×0.28 of the hero's own density) | Bug found by rendering a panel at real game size (340px) after it looked fine at contact-sheet thumbnail size (150px) - the same absolute stroke geometry reads very differently at different zoom levels, so style QA needs at least one look at real render size, not just thumbnails |
| Geometric/Bauhaus and Neon's "pick one part to fall/break" fumble logic was re-verified after the `parts.js` hero rewrite (more parts, new shapes) - still only picks from `ellipse`/`polygon` (fallen) or non-`dot` (broken) shapes | No new bug - confirmed the D-earlier fix (round 1) generalizes correctly to the bigger fox/bird part lists without needing further changes | Re-render of all 6 heroes × 4 seeds in both styles' fumbled variant, round 2 |
| `document.hidden` was `true` at rest in the shared automated browser pane during this round's testing (confirmed directly, matching the rhythm-engine agent's own finding) | Every `tools/styles.html` timing measurement in this session's report is a **direct `performance.now()` delta around a synchronous render call** (no `setTimeout`/rAF in the measured span), so none of it is affected by timer throttling; only the page's own *between-panel* `yieldFrame()` waits (D "rAF hang" fix above) are throttling-sensitive, and those don't affect correctness, only how long the visible contact sheet takes to finish painting | Coordinator's explicit instruction this round to check `document.hidden` before concluding a perf problem, plus a direct in-game `?style=neon&lowend` playthrough logging `totalRenderMs=2265` for a real 12-panel story (well under the 3s budget) as a non-isolated sanity check |
| **Round 3**: a normal screenshot of the live `tools/styles.html` page (a long scrolled DOM with many `<canvas>` elements) comes back blank/timed-out in this hidden pane - confirmed directly by the coordinator's own review notes and reproduced here | Standard method going forward for reviewing any multi-canvas art output in this pane: draw the renders directly onto ONE offscreen `<canvas>` with `drawImage` (+ `fillText` labels), replace `document.body` with just that canvas, then screenshot - a single already-composited canvas paints reliably regardless of pane visibility, where a live multi-element page layout does not | Verified by compositing crayon/neon/geometric's full story arc (7 shots × 2 seeds, plus a dedicated good+fumbled/silhouette/night composite) into single canvases and screenshotting each successfully on the first try |
| Round 2's crayon fix (bound stroke length to the box's smaller dimension) fixed the "big diagonal bars" bug but left stroke COUNT as a small fixed number cut to 0.28× hero density - fine on a 150px thumbnail, but far too sparse at the game's real ~1280px render size, reading as scattered dashes on white paper | `crayonFill`'s stroke count is now computed FROM the shape's area (Poisson coverage model, D54) instead of passed in as a fixed number - self-scaling regardless of render resolution, and background coverage targets raised to 0.8-0.82 (was 0.28) so every layer reads as a filled region; every layer shape also gained a wobbly outline (round 2 only outlined the hero) | Bug found via the coordinator's composited-canvas review this round; re-verified at both 150px (contact sheet) and 260-340px (zoomed inspection) render sizes this time, specifically because round 2's own fix had looked fine on the size it was checked at and wrong at a different one |
| Neon's per-part `drawPart` offset/rng calls (for geometric's new "deliberate offset" primitives, an analogous pattern) must consume rng identically whether or not the offset/effect is applied, across a `fumbled`-independent decision | `offsetIdx`/`breakPart`/`fallenIdx`-style picks are always computed BEFORE the fumbled-conditional branches in the per-part loop, and the offset amount itself is drawn unconditionally from a boolean decided once per render (not re-rolled per variant) - so a good render and its fumbled twin (separate rng instances from the same seed) still agree on which parts are offset/fallen/broken, only the fumble-specific perturbation differs | Applied when adding geometric's deliberate-offset primitives (D66); no dedicated new test beyond re-confirming good/fumbled compositions still match (same pattern already covered by the round-1 fallen/broken-part tests) |
| **Round 4**: `img.decode()` on a blob URL never resolves at all while `document.hidden` is true (confirmed by the coordinator's direct probe: a trivial 640×480 rect's `decode()` was still pending 6s later, hidden, vs. 0ms visible) - `js/story/loader.js`'s old unconditional `await warmup.decode()` hung the "Painting the story..." loading screen forever if the player backgrounded the app while loading, with no way back | `decodeWhenVisible()` (D67) never calls `decode()` while hidden; it waits on `visibilitychange` first, and if the page hides AGAIN before a started `decode()` settles (which can also stall forever), that attempt is abandoned and retried with a fresh `<img>` once visible - `Promise.race()` against a fresh `onceHidden()` per attempt | Verified directly in this hidden pane: (1) a real (unmocked) `loadStory()` call parked with `completed:false`, the honest "Paused while you're away" status logged via `onProgress`, and no error - not a silent hang; (2) mocking `document.hidden` to `false` and dispatching a synthetic `visibilitychange` drove that same call to completion (`completed:true`, all panel/ending steps logged exactly once, in order); (3) a stress test with 3 hidden→visible flips mid-load (`numPhrases:2`, 7 decode calls) also completed cleanly with `waitingCount:3` and every step exactly once, no duplicates and no deadlock, though it took longer wall-clock time than the single-flip case - because mocking `document.hidden` only fools THIS module's own visibility-gate checks, not Chrome's real internal page-visibility state, so `decode()` calls made while "mock-visible" but really-still-hidden were still genuinely slow; a real player's tab has no such split (their `document.hidden` and Chrome's internal state always agree) |
| A promise "must not leak or throw" if the page is hidden forever and the player never returns | Not specially handled beyond what naturally falls out of the design: `decodeWhenVisible()`'s pending `await onceVisible()` is just an ordinary pending Promise parked on one `visibilitychange` listener - no timer, no poll, no retry-storm. If the tab is ever actually closed the browser discards the whole page (listener included) the normal way; if it just sits hidden indefinitely, the loading screen simply keeps waiting (matching "resumes on its own, no tap needed" - there is nothing TO clean up because nothing is running) | Reasoned through, not separately tested (this codebase has no page-teardown/SPA-unmount path to exercise - `loadStory()` is called once per game from a plain multi-screen static site) |
| **M1.6**: `getImageData()` on a GPU-backed canvas costs 9-13ms at 320×230 (vs. ~1.1ms for the whole LUT-based quantize loop it feeds) - the LUT fix alone wasn't enough to hit budget once resolution grew, because the READBACK, not the search algorithm, was now the bottleneck | `small.getContext('2d', { willReadFrequently: true })` (D73) keeps the internal canvas CPU-backed from creation, which needs no GPU sync for `getImageData()`/`putImageData()` | Found by inserting temporary `performance.now()` checkpoints around `getImageData`/the dither loop/`putImageData` separately - the LUT loop itself was already fast, so the aggregate 12-14ms panel cost didn't move until this was found and fixed. **Reusable lesson**: when a "fix the algorithm" change doesn't produce the expected speedup, profile the SURROUNDING calls (canvas readback, allocation, layout) before assuming the algorithm fix was ineffective |
| **M1.6**: re-importing `js/story/styles/index.js` with a cache-busting query string (`?v=N`) does NOT reload `index.js`'s own static `import pixel from './pixel.js'` (no query string) if `./pixel.js` was already in this tab's module registry from an earlier import - every subsequent `getStyle('pixel')`/`STYLES` access silently keeps using the STALE module, even though a direct `fetch('/js/.../pixel.js?v=N', {cache:'no-store'})` in the same tab correctly shows the file's current on-disk content | No code fix (this is normal ES-module resolution behaviour - a module specifier's cache key is its resolved URL, and `./pixel.js` resolves to the same URL every time regardless of what query string the IMPORTING module was fetched with). **Verify-after-edit in this project now means: a full page navigate (not a `?v=` bump) before trusting any post-edit measurement or screenshot that goes through `styles/index.js` or any other file that re-exports an edited module** | Caught by numerically sampling hero-body pixel colours after a shading fix and finding the OLD (pre-fix) ramp shades still present, despite a `fetch` of the same file (bypassing the module cache) showing the new source; a full `navigate()` to the same URL immediately showed the fix taking effect. Everything measured or screenshotted in this session AFTER this was found used a fresh navigation first; measurements taken between the shading edit and this discovery were the OLD code and were re-taken |
| **M1.7** Bluetooth earphones (~250ms latency) | Accepted and compensated (D83), and all judging and visuals use the heard clock (D84) | `tests/calibration_test.mjs` (a deterministic symmetric-jitter case at +250/+400 passes within 3ms, +500/−250 fail, plus a seeded noise case at ±25ms). `tests/judging_e2e_test.mjs` Part 4 (no premature auto-miss at a +300ms offset). Browser: the real Recalibrate flow with taps at +250ms (jittered) stored **+252ms, spread ±15ms** and showed the Bluetooth note. Echo Birds practice taps at +250ms scored "Nice! −2/−4ms" and the markers turned --hit |
| Taps during the calibration count-in | Ignored by time (`isCountInTap`), so they never count toward the 12 | Unit test, plus the browser run above (2 count-in taps sent, all 12 recorded taps kept) |
| An offset near the old pairing boundary (≥300ms at 100bpm) | 80bpm plus the asymmetric window: late taps up to +525ms pair with the right click | Pure-function test: +0.65 beat pairs to the earlier click, −0.25 beat to the next |
| Leaving calibration mid-run | Back aborts, silences scheduled clicks and saves nothing. Play → first calibration → Back does not start a run | Browser: the stored calibration was byte-identical after Back, and the screen returned to the menu |
| A pause landing after the raw song end but before the offset-adjusted end | `pauseNow` ignores it (the song is effectively over) | Code review |
| Pause at the exact instant the heard-clock phrase index lags the raw one | **Known, not fixed:** `resumeNow` might discard (rather than force-complete) a panel that started in raw time but was never shown. There's no crash, only a possibly skipped reveal | Flagged by Sonnet. It's a rare window of at most one offset |
| Swing Samurai: the `bonk` cue glyph and the fake-note marker are both ✕ | The cue shows as a circled ✕ ("listen") a beat before a bare ✕ ("don't tap"). Accepted for now | — |
| `?autoplay=events` with a nonzero stored offset | Its synthetic taps don't add the offset back (unlike `?autoplay=perfect`), so they read early. Use `?autoplay=perfect` for testing | Noted by Sonnet. Pre-existing |
| A retried `plays` upload hitting the `(user_id, client_id)` unique key | POST to `/rest/v1/plays?on_conflict=user_id,client_id` with `Prefer: resolution=ignore-duplicates`, using a named unique constraint. Without `on_conflict`, PostgREST targets the primary key and returns 409, leaving the outbox stuck forever | Found in Opus review. `tests/auth_test.mjs` asserts the URL and the Prefer header. The live Supabase check is still pending (it needs the user's project) |
| A practice round played while the tab was backgrounded | Interrupted: not counted, "Paused - come back to continue", and re-run once the page is visible and focused (never a false pass, never a background audio loop) | `tests/practice_test.mjs` (pure outcome rules). Browser: Try again restarts cleanly |
| Offline navigation with a query string (`/?seed=42`, `?game=`, `?debug`, `?autoplay=`) | The worker matches navigations with `ignoreSearch: true`, so they get the cached `./` | Browser check: stopped the dev server, loaded `/?sw=on&seed=42&debug`, and the page came from cache (CSS applied, menu filled in by `main.js`). Pressing Play then composed the song, rendered the story and reached the play screen with no server |
| A deploy after files change but without re-stamping | Browsers would keep the old cache forever, because `sw.js` bytes didn't change | `tests/sw_test.mjs` (fails with "run `node tools/stamp-sw.mjs`"; confirmed failing before the first stamp and passing after) |
| An update arrives while a game is open | `skipWaiting` + `clients.claim`, and the old cache is deleted. Safe because the page statically imports everything at load (no dynamic imports), so an open tab never needs another file | Browser check: a probe edit + re-stamp + `reg.update()` left exactly one cache, `tapestry-<new>`, containing the changed file (the probe was reverted and re-stamped afterwards) |
| Stale modules on the dev server | No worker on localhost unless `?sw=on`, and `?sw=off` unregisters it | Browser check: plain `localhost:8080` gave 0 registrations and 0 caches, and `?sw=off` removed an existing registration |
| CRLF (Windows checkout) vs LF (deployed) changes the hash | `stamp-sw.mjs` normalises CRLF→LF before hashing | Code review |
| **Tooling**: the supercharge `drift-check.ps1` port must match the `.sh` semantics: skip bare filenames, suffix-match paths against `git ls-files`, take the last `:`-segment as the symbol, use a literal substring match, same `--json` shape, and exit 2 when not a git repo | Ported one-to-one | Tested with a fake `git.cmd` shim, since git isn't installed: a 6-ref fixture gave 4 refs scanned (bare filename skipped), `Widget::render` resolved, 1 DEAD-PATH and 1 DEAD-SYMBOL reported, exit 1, JSON matched. Without git it exits 2. ~~`--selftest` has not been run~~ **Resolved**: with WSL git, the upstream `drift-check.sh --selftest` (run in WSL) and the `drift-check.ps1 --selftest` port (via the WSL-git fallback) both print `selftest OK`. Invoking the `.sh` by bare relative name fails because its self-test re-invokes `$0`, so always call it by full path |
| `js/config.js` left unconfigured (empty `SUPABASE_URL`/`SUPABASE_ANON_KEY`, the shipped default) | Every `js/net/*` call checks `isConfigured()` first; `js/net/accountUi.js` shows "Accounts not set up" on the account chip/screen and both step-free `#screen-account`/`#screen-progress` bodies, and never calls `fetch()`. Guest play is byte-for-byte what it was before this feature existed | `node --check` + the full `tests/run.mjs` suite pass against the unconfigured default; `js/net/auth_test.mjs`'s request builders exercise the (empty-string) URL/key `js/config.js` ships with, so the pure request shape is checked either way |
| A finished play happens while **offline but signed in** | `accountUi.recordPlay()` still calls `storage.enqueuePlay()` unconditionally (only the immediate best-effort flush is skipped when `navigator.onLine===false`) - the play is never lost, just queued until the next menu visit's `onMenuShown()` → `sync.syncNow()` → `flushOutbox()` | `tests/merge_test.mjs`'s "end-to-end outbox lifecycle" case: enqueue → failed flush (nothing lost) → successful flush (drains) → enqueue again (starts clean) |
| A retried flush of the same outbox (e.g. the response to a successful insert never arrived, so the client retries) | Server-side: `plays.client_id` is `unique(user_id, client_id)` plus `Prefer: resolution=ignore-duplicates` (D93) - PostgREST silently skips the already-inserted rows instead of erroring or duplicating. Client-side belt-and-braces: `enqueueOutbox`/`dedupeOutbox` also dedupe by `client_id` before anything is ever sent | `tests/merge_test.mjs` (`dedupeOutbox`, `enqueueOutbox` dedupe cases) covers the client side; the server-side guarantee is the `unique` index + `Prefer` header in `supabase/schema.sql`/`js/net/sync.js` themselves (no live Supabase project in this session to hit for real - see Open questions) |
| A guest (never signed in) finishes a play | `accountUi.recordPlay()` returns immediately (`storage.getSession()` is null) - nothing is queued, nothing is synced. Local progression (`storage.markCleared`/`setLevel`/etc., in `js/main.js`'s existing `applyProgression()`, untouched by this session) is completely unaffected | Code review - `recordPlay`'s early-return is unconditional on `!session` |
| A `401` arrives mid-request (token revoked, or expired exactly between the proactive refresh check and the request landing) | `authorizedFetch()` refreshes once and retries the SAME request once with the fresh token; a second `401` (or a failed refresh) is returned to the caller as-is, which every caller already treats as a normal failed-request case | Code review (D97) - no live Supabase project in this session to reproduce a real 401 against (see Open questions) |
| `progress` vs `plays` upsert conflict resolution needing to differ | See D93 - `merge-duplicates` for `progress` (RLS allows update), `ignore-duplicates` for `plays` (RLS is insert+select only, no update policy at all) | Traced directly against `supabase/schema.sql`'s own policies while writing `js/net/sync.js` |

## Session status (accounts & cross-device progress, section C, this session)

Implemented per the (corrected) plan: `js/config.js` (unconfigured by default), `js/net/auth.js` (pure request builders + refresh-timing + error-mapping, separate from the fetch-calling functions), `js/net/sync.js` (pull/push `progress`, insert `plays`, read/patch `profiles`, all REST-over-fetch), `js/net/merge.js` (pure `mergeProgress` + outbox enqueue/dedupe/flush-bookkeeping), `js/net/accountUi.js` (wires the account chip + `#screen-account` + `#screen-progress`), `supabase/schema.sql` (`profiles`/`progress`/`plays`, RLS on all three, the profile-on-signup trigger, `updated_at` triggers), and a README "Setting up accounts (Supabase)" section. `js/storage.js` gained session/outbox/device-id/progress-snapshot exports; every existing export still works unchanged. `js/main.js` touched at exactly the 5 points in D90 - no gameplay logic (practice, end-screen miss breakdown, `js/games/*.js`) was touched, matching the parallel rhythm-engine agent's ownership of those files.

**Verified this session** (Node only, no browser/server/git, per the task): `node --check` clean on every new/changed `.js` file (`js/config.js`, `js/net/{merge,auth,sync,accountUi}.js`, `js/storage.js`, `js/main.js`, `tests/{merge,auth}_test.mjs`, `tests/run.mjs`). `node tests/run.mjs`: **all 7 test files pass**, including the 2 new ones (`merge_test.mjs`: merge rules + idempotence + union-dedupe + the full outbox lifecycle with a stubbed transport; `auth_test.mjs`: every request builder's exact URL/method/headers/body, `needsRefresh`'s 60s-margin boundary, and `mapAuthError`'s message mapping) and the pre-existing `dom_ids_test.mjs` (51 ids, including every new `account-*`/`progress-*`/`btn-account-*`/`btn-progress-*` id). `node tools/stamp-sw.mjs` re-stamped `sw.js` (46 assets, including the 5 new `js/` files) after which `sw_test.mjs` passes too.

**Not verified this session** (no live Supabase project, no browser, per the task's explicit "no browser, no servers"): the actual sign-up/sign-in/verify/refresh/set-password/sign-out round trip against a real Supabase project, the RLS policies actually blocking a signed-out anon read, a real cross-device sync (two tabs/profiles), and a real offline→online outbox flush against the network. These are exactly the "Verification" bullets the plan itself lists as manual/user-driven for section C - see Open questions below.

## Session status (rhythm engine side of M1.5, this session)

Implemented plan steps 1–4: generic chart model (`js/chart.js`, `js/songform.js`), input holds/fakes (`js/input.js`), `js/audio/sfx.js` + `js/audio/voices.js`, the six minigame modules + `js/games/remix.js`/`js/games/index.js`, the learnability lint + `tools/lint.html`, the practice screen, progression/remix in `js/main.js`, mascots + judgement polish in `js/render/stage.js`. The baked per-note melody (old `synth.js:197-205`) is removed; cues are baked per-game via each module's `soundPack()`; response sfx are pre-rendered buffers played live on judged hits. `stylePrefs` is threaded from every game module through to `loadStory()`. `js/story/**` and `tools/styles.html` were not touched.

`tools/lint.html` reports **0 violations across 4200 charts** (7 "games" incl. remix × 12 levels × 50 seeds). Browser-verified per D48's sampled matrix — all runs reached the expected ending with no console errors. 375px layout has no horizontal scroll; the mascot/caption overlap found during that check (D47) is fixed. Level persists across a real page reload.

**Round 2 (coordinator review + fixes):** the coordinator verified structurally by importing `songform`/`games` directly (no browser) - 525 charts, 0 lint violations, song length a consistent 67-69s, difficulty knobs ramping as intended. Two fixes made: the remix-with-no-cleared-games silent chart (see edge-case table) and the bounce/ready/(pump, proactively) density gap (see edge-case table); `tools/lint.html` re-verified at **0 violations across 4800 charts** (the original 4200 plus a dedicated remix-fresh-profile pass). Both fixes and all re-measurements were done via a **direct Node import of `songform.js`/`games/index.js`** (no DOM/audio dependencies in either file), not the browser pane - this sidesteps the pane's `document.hidden` timer throttling entirely for anything about chart *structure* (note counts, lint, duration).

On the `document.hidden` question specifically: confirmed and root-caused this round. `document.hidden` reads `true` at rest in this harness and only `false` for a moment right after a `computer` tool action (click/navigate/resize). A direct probe (`await` two consecutive `requestAnimationFrame` callbacks) **timed out after 45s** with the tool itself reporting "The Browser pane is currently hidden" - `requestAnimationFrame` isn't just throttled here, it's fully suspended while hidden. Two live `?autoplay=events` runs (fresh tabs, `js/story/**` collision below not present) stalled on the play screen at exactly the note count their chart's density measurement predicts (30/30 for bounce L5 seed 9, matching its measured 27-33 range) and never reached the end screen even after resize/interaction nudges - i.e. `js/main.js`'s `frame()` loop (rAF-driven: `judge.update`, `stage.tick`, and the `nowCtx >= absoluteEndTime` completion check) genuinely stopped ticking once hidden, mid-song.
Concretely, this means: **judging itself is unaffected** - every one of those judged taps landed in the tens-of-ms Perfect/Good range (real `pointerdown` events call `judge.judgeTap` synchronously, no rAF/setTimeout involved), so the accuracy/timing-window logic is sound. What stalls is **rAF-driven progression and completion detection** - a real player's tab is visible by definition while they're tapping, so this never manifests for them, but it means **no wall-clock browser verification (round 1's 18 `?autoplay=events` + 2 `?autoplay=miss` runs, or the round-2 re-checks above) can be fully trusted to reach its end screen in this specific harness state** - only that judging along the way was clean. Round 1's runs DID all reach their end screens correctly, which means the pane was genuinely foregrounded for at least most of that session; whatever differs about the harness's current state is outside this session's control. The one thing round 2 could and did fully re-verify with confidence is everything structural (chart shape, note density, lint) via direct Node import of `songform.js`/`games/index.js` - deliberately chosen for round 2's fixes *because* it sidesteps rAF/`document.hidden` entirely.
A separate, unrelated collision was also observed and self-resolved during this check: `js/story/styles/{neon,geometric}.js` were mid-rename (still calling `scene.js`'s old `computeLayout` export) for several minutes of real time before the art agent's own in-flight edit landed - not touched, not ours to fix, purely a shared-repo timing coincidence with this round's testing window.

**Round 3 (backgrounding / pause-resume):** added `js/pause.js` (backgrounding detection) and `js/songform.js planPauseResume()` (pure resume-point/skip/time-shift math), wired into `js/main.js` (`pauseNow`/`resumeNow`), plus a "Paused / Tap to resume" overlay (`index.html`, `css/game.css`). See D49-D55 and the two new edge-case rows for the design.

Verified three ways. (1) A 72-assertion direct Node import test of `planPauseResume` against real charts from all 6 games × 3 levels × 4 pause scenarios (pause-before-a-phrase, mid-phrase, in-the-outro, already-judged notes never overwritten) - this caught and fixed a real off-by-one (`resumeIdx` computed one past its valid range when pausing in the outro) before it ever reached the browser. (2) A synthetic `visibilitychange`/`blur`/`focus`/`pagehide` dispatch sequence against a live import of `js/pause.js` alone (`document.hidden` mocked via `Object.defineProperty`, since it's a read-only getter), confirming its trigger-once-per-away-spell dedup and `detach()` cleanup - exact expected call sequence (`visibilitychange`, `blur`, `pagehide`, nothing after `detach()`). (3) A full live run: loaded a real game (Bouncy Ball, level 1), let 3 real synthetic taps go through (ignored - not timed to real notes, expected), dispatched a real `window` `blur` event mid-song, and confirmed via the `?debug` log that it actually paused ("paused @ 33.280s (blur)") with the overlay visibly showing "Paused / Tap to resume" in the rendered page text; clicking the overlay correctly resumed ("resumed - skipped 7 note(s), restarting at phrase 3 after a 4-beat count-in") with no console errors. This also caught a real bug via code review before the live run: `detachInput` was still declared `const` in `js/main.js` even though `resumeNow` needs to reassign it - `node --check` can't catch a const-reassignment (it's a runtime `TypeError`, not a syntax error), so this would only have surfaced on an actual resume. Fixed to `let`.

Continued playback after resume (reaching the end screen) hit the same pane-`document.hidden`/rAF-suspension issue as round 1 and round 2 - not a regression, and unsurprising since `frame()`'s rAF loop is exactly what stalls in that state (see round 2's entry above); a separate attempt at a fresh run also hit a transient ~1 minute stall in the LOADING screen itself (pre-existing, unrelated `js/story/loader.js` code, unaffected by this round's changes) that cleared on its own on retry, reinforcing that these are environment-level hiccups rather than anything specific to this feature. `node --check` clean on every touched file; `tools/lint.html` still 0 violations across 4800 charts (this round's changes don't touch chart composition).

## Session status (M1.6: playability fixes - calibration, feedback, guides, practice)

Implemented sections A-D per the rewritten plan (calibration correctness, self-diagnosing mistiming, sound+visual guides, and a practice screen that actually teaches). New files: `js/calibration.js` (pure calibration math). Changed: `js/main.js` (calibration flow, guides wiring, self-diagnosis, practice overhaul), `js/input.js` (ignored-tap diagnostics), `js/storage.js` (version bump + guides pref), `js/render/stage.js` (cue glyph/name, guide ring pool), `js/audio/sfx.js` (`renderGuidePack`/`scheduleBuffer`), all 6 games' `cueTypes` (added `glyph`), `index.html`/`css/game.css` (calibrate detail, menu calibration/guides display, practice timeline/phases/tally, end-screen timing/recalibrate, how-to legend). Section E (pixel art favouring/resolution) is explicitly the art agent's - not touched.

**The root-cause fix was verified FIRST, before any other work, per the task's own instruction.** `js/calibration.js`'s regression test (`calibration_test.mjs`, direct Node import, 9 scenarios: clean run, missed-first-tap [the exact bug trigger], extra tap, double tap, ±80ms jitter, out-of-range, too-few-taps, single-outlier, verify residuals) - PASS. A second test (`judging_e2e_test.mjs`, plan section F's "test that would have caught the bug") drives `createJudge` directly at known offsets (±30ms → Perfect/Good; ±600ms → ignored with `deltaMs` reading ~±600ms) - PASS, and along the way caught a SECOND real bug (D58: the diagnostic nearest-note lookup trusting a bad hint at a search-boundary edge case) before it ever reached the browser. Both are now permanent regression tests any future change to this path should keep passing.

Live-verified in the browser (a fresh tab, calibrate screen only - see below for why Play/Practice couldn't be): dispatched 12 real synthetic `pointerdown` events at a consistent ~-70ms offset with realistic timing jitter through the ACTUAL menu → calibrate → begin flow; the app correctly paired them, rejected no outliers, stored `offset=-67.5ms, spread=14.2ms` in localStorage, advanced to the 4-tap verify step, and returned to the menu showing "Calibration: -68ms (spread ±14ms)" / "Recalibrate timing" - exactly per spec, with zero console errors. `storage.migrateIfNeeded()` correctly set `ord.storageVersion=2`. 375px menu and calibrate screens: no horizontal scroll.

**What could not be verified live**: the Play and Practice screens (guides, the new corner cue icon/name, the practice timeline/phases/tally) never became reachable this session - `js/story/loader.js`'s `decodeWhenVisible()` (the OTHER agent's own M1.5-round-4 fix, not touched here) correctly waits for genuine page visibility before calling `img.decode()`, and this harness's pane reports `document.hidden` in a way that never gave it a real visible window in dozens of attempts across multiple fresh tabs, including several minutes of waiting and both real and mocked `visibilitychange` dispatches (mocking only fools the module's own gate, not Chrome's actual internal decode scheduling - confirmed by the loading screen staying frozen even with the mock in place). This is the same environment class as round 2's rAF-suspension finding, one level further upstream. Everything on that path was instead verified via code review (traced `stage.tick`'s new `resolveCueType` argument, `scheduleGuideTicks`'s judged/fake filtering, the guide pool's live-note-reference design across a pause resume) plus the structural regression tests above, which exercise the same `createJudge`/`planPauseResume` machinery the Play screen's guide scheduling and self-diagnosis are built on. A live Play/Practice playthrough (guides on, then off, 375px and desktop, by hand) is the one item from plan section F this session could not complete and should be re-run once the pane is genuinely visible.

## Session status (art styles side of M1.5, round 2)

Rebuilt `js/story/styles/scene.js` from a fixed sky/ground/sun/hero layout into a full composition system (`composeScene()`): 6 shot types (wide/mid/closeup/silhouette/highangle/detail) weighted per story beat kind, 4 time-of-day variants + 4 weather conditions, 6 environment types (sea/forest/hills/mountains/town/path) each built as a far/mid/near parallax layer stack of plain-data shapes, and richer hero placement (thirds, facing, near/far size, a `silhouette` flag). All 7 style modules were rewritten to draw this through their own existing shape-fill primitive rather than a shared renderer - see D58. `parts.js`'s fox/bird silhouettes were redrawn bigger and more distinct (D60). Pixel art, crayon and ink each got a fidelity pass (D61 + edge-case table for pixel/crayon; ink got denser hatch/cross-hatch, stippling, a heavier hero contour and bolder accent fill - see `js/story/styles/ink.js`). `tools/styles.html` now shows the full story arc (setout → journey → obstacle → climax → good/bad ending) for 2 seeds (fox, bird) per style, and measures both a single full-size panel and a whole low-end 7-shot "story" render per style.

Verification: `node --check` clean on all 13 files in `js/story/**`. Direct (non-browser-timer) `performance.now()` measurements for a realistic low-end 7-shot story, forced per style: 9.2-114.8ms (watercolor 22.3, papercut 11.4, pixel 114.8, ink 15.1, crayon 16.4, neon 9.2, geometric 9.2) - all far under the 3s budget; single full-size-panel normal-mode renders are all under 45ms except pixel (~40-90ms, still trivial). Once the rhythm-engine agent's `chart.js`/`main.js` stabilized mid-session (see their note above on the transient `computeLayout` collision - also resolved from this side once every style module's rename landed), two live in-game playthroughs were run directly in the browser pane: `?style=crayon&autoplay=perfect` played a full ~85s song to completion ("A good ending!", Perfect 29/Miss 0, 100% accuracy) at a 375px viewport with zero console errors, and `?style=neon&lowend&autoplay=perfect` loaded its cover panel with `[story] ... lowEnd=true panels=12 totalRenderMs=2265` logged - both are real, non-isolated confirmations (not just the contact sheet) that the composition system and a cost-3 style meet the low-end budget and render correctly through the actual loader → stage.js pipeline. `document.hidden` was confirmed `true` at rest in this pane during testing (per the rhythm-engine agent's finding above); every timing number reported here is a direct `performance.now()` delta around a synchronous render call, not a wall-clock wait, so none of it is affected by that throttling - the one exception (`tools/styles.html`'s own between-panel pacing) only affects how long the visible sheet takes to finish painting, not any measurement in this log.

## Session status (art styles side of M1.5, round 3)

Coordinator review (self-composited the contact sheet's canvases to work around the hidden pane) called out watercolour/papercut/pixel/ink as good, and three styles as still weak, all in their BACKGROUND: crayon read as scattered coloured dashes on white, neon read as muted olive/brown instead of synthwave, geometric's hero was "just the ordinary silhouette in flat colour." Root causes and fixes: see D63-D66. In every case the fix was to make that style's technique less literal/more idiom-specific for its BACKGROUND (crayon's stroke count now scales with area instead of being a flat count; neon's sky/grid/sun are a fixed vivid preset instead of a mix of the story's own often-pastel colours; geometric's hero is rebuilt from bold primitives instead of tracing the shared part list's literal shapes) - matching the cross-cutting note that pixel art already treats the shared shape list as raw material for its own idiom rather than rendering it near-verbatim.

Verification used the same compositing technique the coordinator used: rather than screenshotting the live (hidden-pane) contact-sheet page, a script draws several styles' renders directly onto ONE `<canvas>` with `drawImage` + text labels, replaces `document.body` with just that canvas, and a single screenshot of it always succeeds regardless of pane visibility - this is now the standard review method for this project's browser pane, not just a one-off workaround. Reviewed: crayon/neon/geometric across the full story arc (multiple seeds/heroes) plus a dedicated good+fumbled, silhouette-shot, night-weather composite - confirmed dense filled crayon backgrounds with visible wobbly outlines, a genuinely dark indigo-to-magenta neon sky with starfield/grid/banded horizon sun/glowing ridgelines, and a geometric hero built from overlapping circles/triangles/half-circles in flat colour. Timing re-measured after the fix: crayon's low-end 7-shot story time rose from 16.4ms to 87-116ms (the coverage-based stroke count is deliberately more expensive than the old fixed count) but is still ~3-4% of the 3000ms budget; neon and geometric were both still under 15ms. `node --check` clean on all 13 files in `js/story/**`.

## Session status (art styles side of M1.5, round 4)

Round 3's approval came with one new, higher-severity bug found directly by the coordinator: `js/story/loader.js`'s `await warmup.decode()` never resolves while `document.hidden` (Chrome defers all `HTMLImageElement` decode work for hidden pages), so a player who taps Play and backgrounds the app mid-load gets a loading screen that hangs forever with no way back. Fixed in `js/story/loader.js` only (`canvasToDecodedUrl`/`decodeWhenVisible`/`onceVisible`/`onceHidden` - see D67/D68 and the new edge-case rows): the decode phase now waits for real visibility before ever calling `decode()`, reports an honest "Paused while you're away" status through the existing `onProgress` callback (no `js/main.js` change needed - it already renders whatever label string it's given), and is robust to any number of visibility flips via a race-and-retry loop. `?debug` now logs `decodeMs`/`visibilityWaitMs` alongside the existing render timing.

Verified directly in this hidden pane, per the coordinator's ask: a genuine (unmocked) `loadStory()` call parks cleanly (`completed:false`, the "Paused..." status observed via a progress-callback spy, no error) rather than hanging silently; mocking `document.hidden` to `false` and dispatching a synthetic `visibilitychange` drives that same call to completion with every step logged exactly once, in order; a 3-flip stress test (7 decode calls across a 2-phrase story) also completed cleanly with no duplicate or skipped steps. Full detail, including why the mocked test's wall-clock time isn't representative of a real player's experience, is in the edge-case table. `node --check` clean on `js/story/loader.js` (and the rest of `js/story/**`, unchanged this round).

## Session status (art styles side, M1.6 section E)

New milestone: pixel art favoured (~3x weight, D69) at much higher resolution with a shaded ramp palette (D71/D72), with the cost fix (LUT-based quantize, D70) required first per the plan. Touched only `js/story/styles/pixel.js` and `js/story/styles/index.js` (`tools/styles.html` was read but not edited - its existing pixel-specific `image-rendering: pixelated` handling from round 3 already covers the new resolution with no changes needed).

The cost fix alone (LUT) didn't hit budget once resolution actually grew 8x/3x - profiling found `getImageData()`'s GPU-readback sync, not the quantize search, was now the dominant cost; fixed with `willReadFrequently: true` (D73). A first attempt at hero shading (gradient between the ramp steps adjacent to a part's own base shade) was too subtle to read once dithered/quantized and was widened to span the ramp's full dark-to-light extremes (D72) - caught by numerically sampling rendered pixel colours, not just eyeballing screenshots, after a visual check that LOOKED unchanged turned out to be reading a stale cached module (see the new edge-case row - re-importing `styles/index.js` with a `?v=` query does not reload its own un-versioned internal `import './pixel.js'`; only a full page navigation does). All final numbers below are from a freshly-navigated tab, after that was understood.

**Cost** (median of 20 runs, warmed up, full-size 1280x920 panel, canvas released between runs): pixel **5.9ms normal / 3.0ms low-end** - under the pre-change baseline the coordinator gave (8.7/4.5ms) despite 8x/3x more internal pixels and 26 vs. 14 palette colours. Full comparison against all 7 styles, same methodology: watercolour 0.9/0.6, papercut 0.4/0.5, pixel 5.9/3.0, ink 1.5/1.5, crayon 8.1/6.4, neon 0.8/0.8, geometric 0.4/0.5 (crayon and ink are unowned this round and shown only for context - not touched). A whole low-end 10-panel story (20 renders): 63.4ms - 2.1% of the 3s budget; normal-mode equivalent: 140.3ms. Encoded panel size DID grow meaningfully (WebP ~161KB normal / ~124KB low-end for pixel vs. ~16KB/10KB for watercolour) - dithered noise compresses far worse than flat/smooth content - but this doesn't threaten the stated memory budget: DECODED bitmap memory is `finalW*finalH*4` bytes regardless of style (identical across all 7, confirmed unaffected), and the blob itself is only briefly larger (a few hundred KB more across a whole story, trivial against the 60-150MB budget). Flagged as a known tradeoff, not fixed (`loader.js`'s fixed WebP quality 0.88 for all styles isn't mine to change this round).

**Frequency**: 200 seeded `pickStyle()` calls, no `stylePrefs`: pixel 31.0% (one run) / 35.5% (a different seed set) - both bracket the theoretical 33.3% (3-of-9 weight share) well within binomial sampling noise (~200 samples, stderr ≈3.3%) and inside the 30-35% target either way. Confirmed `stylePrefs` combine multiplicatively (a minigame favouring neon 2x raised neon's share without moving pixel's), and low-end pool exclusion (cost-3 styles dropped) raises pixel's share further (39.5%) since it stays IN that pool at cost 2 (D74).

**Look**: a native-size (no CSS rescale) contact-sheet composite across the 7-shot story arc x 2 heroes x good/fumbled, built the same way as the round-3 compositing technique (draw every render onto one `<canvas>`, screenshot that), confirmed: genuinely chunky pixel blocks at every shot type (not just close-ups), dithered sky gradients with visible star sparkle shapes and actual cloud clusters (not dots), textured/foliage ground with a visible ambient-occlusion shadow under the hero, a two-tone sun/moon disc, and - after the shading widen-fix - a clearly readable lit-side/shadow-side gradient plus rim highlight on the hero's main body. Both fox and bird read as distinct silhouettes at small (mid/wide shot) sizes. Fumbled panels show clear glitch-row-shift and palette-shift-band corruption. `node --check` clean on all 13 files in `js/story/**` (only `pixel.js`/`index.js` changed).

---

## Open questions

- Publishing to GitHub Pages has **not** been done. ~~The folder is not a git repository yet~~ It is now a git repo (initialised by the user). The user does all add/commit/push themselves (global rule). Run `node tools/stamp-sw.mjs` before each deploy (D78).
- Timing has only been verified with synthetic taps in the desktop browser pane; it still needs a real playtest on a phone.
- ~~Full in-game `?style=<id>&autoplay=perfect` playtests (art-style side of M1.5) were **not completed**: at review time `js/chart.js` ... didn't export `generateChart`~~ **Resolved this session**: `js/chart.js`/`js/main.js` are stable (rhythm engine M1.5 steps 1-4 complete, see "Session status" above). End-to-end real-game playtests now pass with no console errors at 375px and desktop widths; `?style=` itself wasn't re-swept per-value in this session (that's the art agent's surface), but the loader call path (`stylePrefs` threaded through from every game module) is confirmed wired up and working. **Further resolved (art side, round 2):** `?style=crayon&autoplay=perfect` was played to a real good ending at 375px (see "Session status (art styles side of M1.5, round 2)" above) and `?style=neon&lowend` confirmed a cost-3 style's real in-game load time (2265ms for 12 panels). The remaining 5 styles (watercolor/papercut/pixel/ink/geometric) were exercised through `renderScene`/`renderFumbled` directly (same code path `loadStory()` calls) rather than a full live playthrough each, for time; a full sweep of all 7 via `?style=` would still be worth a follow-up pass.
- Per-game mascot animations are currently generic (one shared bounce/pulse/scale vocabulary across all 6 games, differentiated only by which emoji shows), not the bespoke "bird opens its beak / ball arcs / samurai slashes" animations sketched in the plan. Worth a follow-up pass if mascot expressiveness matters more than current scope allowed.
- ~~The practice screen has no visual metronome...~~ **Resolved in M1.6**: practice now has a timeline strip with a moving playhead (section D), plus the same guide tick sounds as Play.
- ~~M1.6 could not live-verify the Play/Practice screens...~~ **Resolved by the coordinator**: driving the pane with real clicks (a `computer`-tool click makes `document.hidden` false, which is what unblocks `js/story/loader.js`'s visibility-gated `decode()`) got a genuinely-visible pane. Confirmed live: menu shows "Calibration: not set" correctly on a fresh profile, practice reached "Your turn!" with the timeline strip/dots/round-counter/Hear-it-again/Skip all present, Play started with the guide ring pool in place, and a full `?autoplay=events` run scored 30/30 Perfect (offsets 11-32ms, median 20.5ms, 0 ignored) to a good ending with the new `allTapsMedian` stat showing on the debug overlay. Both `calibration_test.mjs` and `judging_e2e_test.mjs` were independently re-run and PASS. **The user's own playtest is still the final confirmation this milestone exists to earn**, but nothing is known to be blocking it.
- Section E (pixel art favoured ~3× + higher resolution + LUT-based quantize) is the art agent's own surface (`js/story/styles/index.js`, `js/story/styles/pixel.js`) - not touched or verified here; see their own session-status entry for that work's status.
- **Accounts (section C) still need the manual Supabase setup + a real end-to-end playtest** - see README.md "Setting up accounts (Supabase)" for the steps (create project, email OTP settings, `{{ .Token }}` template, optional custom SMTP, Site URL/redirect, run `supabase/schema.sql`, paste the URL+anon key into `js/config.js`). Until then `js/config.js` stays unconfigured and the game plays exactly as before (guest-only). Once configured, still unverified: the real code/password sign-up+sign-in+refresh+set-password+sign-out round trip, RLS actually blocking a signed-out anon read (`fetch` with only the anon key should return `[]`), a real two-tab/two-profile cross-device sync, and a real offline→online outbox flush - this session's verification was Node-only (merge/auth logic, `node --check`, `node tests/run.mjs`, `node tools/stamp-sw.mjs`), per the task's explicit "no browser, no servers, no git".
